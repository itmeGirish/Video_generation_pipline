[
  {
    "framesFrom": 0,
    "framesTo": 367,
    "code": "const NARRATION_TEXT = \"To find where your usage really goes, you need one strange fact about how Claude Code works. <pause 0.4s> It has no memory between your messages. Every time you hit enter, Claude wakes up with a completely blank mind. <pause 0.3s> It doesn't remember the last thing it just told you. Not the file it edited, not the plan you agreed on two minutes ago. Nothing. So how does it follow a conversation at all? <pause 0.5s> Simple. It re-reads the entire chat from the very beginning. Every single time. Ten messages in, that means the whole back-and-forth, read start to finish, before it answers a single word. Picture someone with no short-term memory. <pause 0.3s> Before they can say a single sentence, they re-read the whole notebook from page one. That's your conversation. <pause 0.3s> Message two re-reads message one. Message ten re-reads all nine before it. <pause 0.4s> Which means a long chat doesn't cost a little more each turn. <pause 0.3s> It costs more on every single turn, because the pile it re-reads keeps getting taller. <pause 0.6s> That's the number that isn't the price. Now watch what happens when Claude actually does some work.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nvar bs=spring({frame:frame-4,fps,config:{damping:13,stiffness:130}});var pop=interpolate(frame,[R(dur*0.66),R(dur*0.8)],[1,1.4],CLAMP);var popo=interpolate(frame,[R(dur*0.66),R(dur*0.8)],[1,0],CLAMP);var bx=R(w*0.66),by=R(h*0.20);var bw2=R(w*0.13),bh2=R(h*0.10);var bub=React.createElement('div',{style:{position:'absolute',left:bx,top:by,width:bw2,height:bh2,borderRadius:'50%',border:'3px solid '+D.text_dim,backgroundColor:D.surface,opacity:bs*popo,transform:'scale('+(bs*pop)+')'}});var d1=React.createElement('div',{style:{position:'absolute',left:bx-R(w*0.01),top:by+bh2,width:R(w*0.016),height:R(w*0.016),borderRadius:'50%',border:'3px solid '+D.text_dim,backgroundColor:D.surface,opacity:bs*popo}});return React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,fontFamily:D.font_display}},React.createElement(Kit.Stack,{sheets:[{color:D.cyan,label:'you',h:0.03},{color:D.violet,label:'claude',h:0.03}],x:0.36,y:0.62}),bub,d1,React.createElement(Kit.ClaudeOrb,{mood:'idle',x:0.68,y:0.5,scale:1.1}),React.createElement(Kit.Battery,{rem:0.84}));",
    "audio_anchor": "no memory",
    "anchor_mode": "land",
    "source_headline": "No memory."
  },
  {
    "framesFrom": 367,
    "framesTo": 784,
    "code": "const NARRATION_TEXT = \"To find where your usage really goes, you need one strange fact about how Claude Code works. <pause 0.4s> It has no memory between your messages. Every time you hit enter, Claude wakes up with a completely blank mind. <pause 0.3s> It doesn't remember the last thing it just told you. Not the file it edited, not the plan you agreed on two minutes ago. Nothing. So how does it follow a conversation at all? <pause 0.5s> Simple. It re-reads the entire chat from the very beginning. Every single time. Ten messages in, that means the whole back-and-forth, read start to finish, before it answers a single word. Picture someone with no short-term memory. <pause 0.3s> Before they can say a single sentence, they re-read the whole notebook from page one. That's your conversation. <pause 0.3s> Message two re-reads message one. Message ten re-reads all nine before it. <pause 0.4s> Which means a long chat doesn't cost a little more each turn. <pause 0.3s> It costs more on every single turn, because the pile it re-reads keeps getting taller. <pause 0.6s> That's the number that isn't the price. Now watch what happens when Claude actually does some work.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nvar fly=spring({frame:frame-3,fps,config:{damping:14,stiffness:120}});var inx=(1-fly)*-R(w*0.25);var ghosts=[0,1].map(function(i){var t=interpolate(frame,[R(dur*0.2)+i*6,R(dur*0.6)+i*6],[0,1],CLAMP);var gy=-t*R(h*0.18);var go=interpolate(t,[0,0.3,1],[0,0.5,0],CLAMP);return React.createElement('div',{key:i,style:{position:'absolute',left:R(w*0.6)+i*R(w*0.02),top:R(h*0.42)+gy,width:R(w*0.06),height:R(h*0.024),borderRadius:R(w*0.004),backgroundColor:i?D.violet:D.cyan,opacity:go}});});var sheet=React.createElement('div',{style:{position:'absolute',left:R(w*0.30)+inx,top:R(h*0.5),width:R(w*0.2),height:R(h*0.03),borderRadius:R(w*0.004),backgroundColor:D.cyan,opacity:fly,boxShadow:'0 4px 14px rgba(0,0,0,0.35)'}});return React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,fontFamily:D.font_display}},React.createElement(Kit.Stack,{sheets:[{color:D.text,label:'file',h:0.028},{color:D.violet,label:'claude',h:0.028}],x:0.36,y:0.66}),sheet,React.createElement('div',{},ghosts),React.createElement(Kit.ClaudeOrb,{mood:'lost',x:0.68,y:0.5,scale:1.1}),React.createElement(Kit.Battery,{rem:0.8}));",
    "audio_anchor": "blank mind",
    "anchor_mode": "through",
    "source_headline": "A blank mind."
  },
  {
    "framesFrom": 784,
    "framesTo": 1336,
    "code": "const NARRATION_TEXT = \"To find where your usage really goes, you need one strange fact about how Claude Code works. <pause 0.4s> It has no memory between your messages. Every time you hit enter, Claude wakes up with a completely blank mind. <pause 0.3s> It doesn't remember the last thing it just told you. Not the file it edited, not the plan you agreed on two minutes ago. Nothing. So how does it follow a conversation at all? <pause 0.5s> Simple. It re-reads the entire chat from the very beginning. Every single time. Ten messages in, that means the whole back-and-forth, read start to finish, before it answers a single word. Picture someone with no short-term memory. <pause 0.3s> Before they can say a single sentence, they re-read the whole notebook from page one. That's your conversation. <pause 0.3s> Message two re-reads message one. Message ten re-reads all nine before it. <pause 0.4s> Which means a long chat doesn't cost a little more each turn. <pause 0.3s> It costs more on every single turn, because the pile it re-reads keeps getting taller. <pause 0.6s> That's the number that isn't the price. Now watch what happens when Claude actually does some work.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nvar scan=interpolate(frame,[R(dur*0.18),R(dur*0.74)],[0,1],{extrapolateLeft:'clamp',extrapolateRight:'clamp',easing:Easing.inOut(Easing.cubic)});var cost=0.2;var rem=0.8-scan*cost;var sheets=[{color:D.text,label:'file.ts',h:0.028},{color:D.cyan,label:'you',h:0.028},{color:D.violet,label:'claude',h:0.028},{color:D.cyan,label:'you',h:0.028},{color:D.text_dim,label:'log',h:0.028}];var stackTop=0.72-(sheets.length*0.032);var rp=spring({frame:frame-R(dur*0.78),fps,config:{damping:12,stiffness:120}});var reply=(frame>dur*0.76)?React.createElement('div',{style:{position:'absolute',left:R(w*0.63),top:R(h*0.4),width:R(w*0.1),height:R(h*0.03),borderRadius:R(w*0.004),backgroundColor:D.violet,opacity:rp,transform:'scale('+rp+')'}}):null;return React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,fontFamily:D.font_display}},React.createElement(Kit.Stack,{sheets:sheets,x:0.34,y:0.72,delay:2}),React.createElement(Kit.ScanSweep,{progress:scan,x:0.235,y:stackTop,w:0.21,h:0.72-stackTop,color:D.cyan}),React.createElement(Kit.ClaudeOrb,{mood:'scanning',x:0.7,y:0.46,scale:1.1}),reply,React.createElement(Kit.Battery,{rem:rem}));",
    "audio_anchor": "re-reads the entire",
    "anchor_mode": "through",
    "source_headline": "It re-reads everything."
  },
  {
    "framesFrom": 1336,
    "framesTo": 1566,
    "code": "const NARRATION_TEXT = \"To find where your usage really goes, you need one strange fact about how Claude Code works. <pause 0.4s> It has no memory between your messages. Every time you hit enter, Claude wakes up with a completely blank mind. <pause 0.3s> It doesn't remember the last thing it just told you. Not the file it edited, not the plan you agreed on two minutes ago. Nothing. So how does it follow a conversation at all? <pause 0.5s> Simple. It re-reads the entire chat from the very beginning. Every single time. Ten messages in, that means the whole back-and-forth, read start to finish, before it answers a single word. Picture someone with no short-term memory. <pause 0.3s> Before they can say a single sentence, they re-read the whole notebook from page one. That's your conversation. <pause 0.3s> Message two re-reads message one. Message ten re-reads all nine before it. <pause 0.4s> Which means a long chat doesn't cost a little more each turn. <pause 0.3s> It costs more on every single turn, because the pile it re-reads keeps getting taller. <pause 0.6s> That's the number that isn't the price. Now watch what happens when Claude actually does some work.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nvar nx=R(w*0.30),ny=R(h*0.26),nw=R(w*0.22),nh=R(h*0.42);var rings=[0,1,2,3,4,5,6].map(function(i){return React.createElement('div',{key:i,style:{position:'absolute',left:nx-R(w*0.008),top:ny+R(h*0.03)+i*R(nh/7.5),width:R(w*0.02),height:R(w*0.012),borderRadius:'50%',border:'3px solid '+D.text_dim}});});var lines=[0,1,2,3,4,5].map(function(i){return React.createElement('div',{key:i,style:{position:'absolute',left:nx+R(w*0.04),top:ny+R(h*0.05)+i*R(h*0.055),width:R(w*0.13)-((i%3)*R(w*0.02)),height:R(h*0.01),borderRadius:R(h*0.005),backgroundColor:D.text_dim,opacity:0.5}});});var flip=interpolate(frame,[R(dur*0.15),R(dur*0.6)],[0,1],CLAMP);var pscale=Math.abs(Math.cos(flip*Math.PI*3));var page=React.createElement('div',{style:{position:'absolute',left:nx+nw*0.5,top:ny,width:nw*0.5,height:nh,backgroundColor:D.surface,borderLeft:'1px solid '+D.text_dim,transformOrigin:'left center',transform:'scaleX('+pscale+')',opacity:0.85}});var pg=React.createElement('div',{style:{position:'absolute',left:nx+nw+R(w*0.01),top:ny+R(h*0.01),fontFamily:mono,fontSize:R(w*0.014),color:D.text_dim}},'p.'+(1+Math.floor(flip*40)));var sb=spring({frame:frame-R(dur*0.65),fps,config:{damping:12,stiffness:120}});var bubble=React.createElement('div',{style:{position:'absolute',left:R(w*0.62),top:R(h*0.3),width:R(w*0.12),height:R(h*0.08),borderRadius:R(w*0.02),backgroundColor:D.violet,opacity:sb,transform:'scale('+sb+')'}});var nb=React.createElement('div',{style:{position:'absolute',left:nx,top:ny,width:nw,height:nh,borderRadius:R(w*0.008),backgroundColor:D.surface,border:'2px solid '+D.text_dim}});return React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,fontFamily:D.font_display}},nb,React.createElement('div',{},lines),page,React.createElement('div',{},rings),pg,bubble,React.createElement(Kit.ClaudeOrb,{mood:'idle',x:0.72,y:0.5,scale:0.9}),React.createElement(Kit.Battery,{rem:0.62}));",
    "audio_anchor": "page one",
    "anchor_mode": "through",
    "source_headline": "The notebook."
  },
  {
    "framesFrom": 1566,
    "framesTo": 1808,
    "code": "const NARRATION_TEXT = \"To find where your usage really goes, you need one strange fact about how Claude Code works. <pause 0.4s> It has no memory between your messages. Every time you hit enter, Claude wakes up with a completely blank mind. <pause 0.3s> It doesn't remember the last thing it just told you. Not the file it edited, not the plan you agreed on two minutes ago. Nothing. So how does it follow a conversation at all? <pause 0.5s> Simple. It re-reads the entire chat from the very beginning. Every single time. Ten messages in, that means the whole back-and-forth, read start to finish, before it answers a single word. Picture someone with no short-term memory. <pause 0.3s> Before they can say a single sentence, they re-read the whole notebook from page one. That's your conversation. <pause 0.3s> Message two re-reads message one. Message ten re-reads all nine before it. <pause 0.4s> Which means a long chat doesn't cost a little more each turn. <pause 0.3s> It costs more on every single turn, because the pile it re-reads keeps getting taller. <pause 0.6s> That's the number that isn't the price. Now watch what happens when Claude actually does some work.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nvar n=Math.max(2,Math.floor(interpolate(frame,[0,R(dur*0.85)],[2,10],CLAMP)));var pal=[D.text,D.cyan,D.violet,D.text_dim];var sheets=[];for(var i=0;i<n;i++){sheets.push({color:pal[i%4],label:'',h:0.026});}var rem=interpolate(frame,[0,dur],[0.9,0.42],CLAMP);var cyc=(frame%26)/26;var stackTop=0.74-(n*0.03);var cnt=React.createElement('div',{style:{position:'absolute',left:R(w*0.5),top:R(h*0.4),fontFamily:D.font_display,fontWeight:700,fontSize:R(w*0.06),color:D.cyan}},'×'+n);var lbl=React.createElement('div',{style:{position:'absolute',left:R(w*0.5),top:R(h*0.5),fontFamily:mono,fontSize:R(w*0.016),color:D.text_dim,letterSpacing:'0.1em'}},'messages');return React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,fontFamily:D.font_display}},React.createElement(Kit.Stack,{sheets:sheets,x:0.32,y:0.74,delay:0}),React.createElement(Kit.ScanSweep,{progress:cyc,x:0.215,y:stackTop,w:0.21,h:0.74-stackTop,color:D.cyan}),cnt,lbl,React.createElement(Kit.Battery,{rem:rem}));",
    "audio_anchor": "all nine",
    "anchor_mode": "through",
    "source_headline": "The pile keeps growing."
  },
  {
    "framesFrom": 1808,
    "framesTo": 2108,
    "code": "const NARRATION_TEXT = \"To find where your usage really goes, you need one strange fact about how Claude Code works. <pause 0.4s> It has no memory between your messages. Every time you hit enter, Claude wakes up with a completely blank mind. <pause 0.3s> It doesn't remember the last thing it just told you. Not the file it edited, not the plan you agreed on two minutes ago. Nothing. So how does it follow a conversation at all? <pause 0.5s> Simple. It re-reads the entire chat from the very beginning. Every single time. Ten messages in, that means the whole back-and-forth, read start to finish, before it answers a single word. Picture someone with no short-term memory. <pause 0.3s> Before they can say a single sentence, they re-read the whole notebook from page one. That's your conversation. <pause 0.3s> Message two re-reads message one. Message ten re-reads all nine before it. <pause 0.4s> Which means a long chat doesn't cost a little more each turn. <pause 0.3s> It costs more on every single turn, because the pile it re-reads keeps getting taller. <pause 0.6s> That's the number that isn't the price. Now watch what happens when Claude actually does some work.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nvar n=Math.floor(interpolate(frame,[R(dur*0.05),R(dur*0.6)],[0,8],CLAMP));var x0=R(w*0.22),y0=R(h*0.72),dx=R(w*0.07),k=R(h*0.011);var dots=[];for(var i=0;i<n;i++){var px=x0+i*dx,py=y0-i*i*k;dots.push(React.createElement('div',{key:i,style:{position:'absolute',left:px,top:py,width:R(w*0.014),height:R(w*0.014),borderRadius:'50%',backgroundColor:D.red,boxShadow:'0 0 '+R(w*0.006)+'px '+D.red}}));}var axx=React.createElement('div',{style:{position:'absolute',left:x0-R(w*0.02),top:y0+R(w*0.01),width:R(w*0.62),height:2,backgroundColor:D.text_dim,opacity:0.5}});var axy=React.createElement('div',{style:{position:'absolute',left:x0-R(w*0.02),top:R(h*0.2),width:2,height:R(h*0.52),backgroundColor:D.text_dim,opacity:0.5}});var hh=spring({frame:frame-R(dur*0.62),fps,config:{damping:11,stiffness:130}});var hx=x0+7*dx,hy=y0-49*k;var green=frame>dur*0.72;var hashn=React.createElement('div',{style:{position:'absolute',left:hx-R(w*0.02),top:hy-R(w*0.03),fontFamily:D.font_display,fontWeight:700,fontSize:R(w*0.06),color:green?D.green:D.text,opacity:hh,transform:'scale('+hh+')'}},'#');return React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,fontFamily:D.font_display}},axx,axy,React.createElement('div',{},dots),hashn,React.createElement(Kit.Battery,{rem:0.4}));",
    "audio_anchor": "every single turn",
    "anchor_mode": "land",
    "source_headline": "The number that isn't the price."
  }
]