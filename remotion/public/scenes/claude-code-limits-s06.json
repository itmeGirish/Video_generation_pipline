[
  {
    "framesFrom": 0,
    "framesTo": 374,
    "code": "const NARRATION_TEXT = \"The biggest usage killer isn't a setting. <pause 0.3s> It's a feeling. The feeling that you're almost there, so you try one more time. It starts with a broad request. <pause 0.3s> \\\"The app feels slow \\u2014 can you make it faster?\\\" No file, no function, no symptom. Just a vibe, handed to a tool that can only act on specifics. With nothing specific to go on, Claude does the only thing it can. <pause 0.3s> It reads everything. Thirty files, just to guess where slow lives. A vague question forces a giant search, and that whole search lands in your context. Its guess is close, but not quite right. <pause 0.3s> So you say \\\"no, not that \\u2014 try again.\\\" And the whole loop runs a second time. Each retry doesn't start clean. <pause 0.3s> It re-reads every file, every failed attempt, every back-and-forth before it. The spiral gets wider each turn. Five retries on a broad request can cost more than fifty precise ones. <pause 0.6s> And it feels productive the entire time, because something is always happening on screen. That's the cruelest part of the spiral. It looks exactly like progress, right up until the limit hits. That's the trap. <pause 0.3s> Vague plus retry is the fastest way there is to drain a window. <pause 0.4s> So what does the opposite look like? Two developers, same task \\u2014 let's run them side by side.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nreturn React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,display:'flex',alignItems:'center',justifyContent:'center',fontFamily:D.font_display}},React.createElement(Kit.DotGrid,{}),React.createElement(Kit.Battery,{rem:0.6}),React.createElement('div',{style:{fontSize:R(w*0.02),color:D.text_dim,fontFamily:mono}},'—'));",
    "audio_anchor": "one more time",
    "anchor_mode": "appear",
    "source_headline": "One more time."
  },
  {
    "framesFrom": 374,
    "framesTo": 790,
    "code": "const NARRATION_TEXT = \"The biggest usage killer isn't a setting. <pause 0.3s> It's a feeling. The feeling that you're almost there, so you try one more time. It starts with a broad request. <pause 0.3s> \\\"The app feels slow \\u2014 can you make it faster?\\\" No file, no function, no symptom. Just a vibe, handed to a tool that can only act on specifics. With nothing specific to go on, Claude does the only thing it can. <pause 0.3s> It reads everything. Thirty files, just to guess where slow lives. A vague question forces a giant search, and that whole search lands in your context. Its guess is close, but not quite right. <pause 0.3s> So you say \\\"no, not that \\u2014 try again.\\\" And the whole loop runs a second time. Each retry doesn't start clean. <pause 0.3s> It re-reads every file, every failed attempt, every back-and-forth before it. The spiral gets wider each turn. Five retries on a broad request can cost more than fifty precise ones. <pause 0.6s> And it feels productive the entire time, because something is always happening on screen. That's the cruelest part of the spiral. It looks exactly like progress, right up until the limit hits. That's the trap. <pause 0.3s> Vague plus retry is the fastest way there is to drain a window. <pause 0.4s> So what does the opposite look like? Two developers, same task \\u2014 let's run them side by side.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nreturn React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,display:'flex',alignItems:'center',justifyContent:'center',fontFamily:D.font_display}},React.createElement(Kit.DotGrid,{}),React.createElement(Kit.Battery,{rem:0.6}),React.createElement('div',{style:{fontSize:R(w*0.02),color:D.text_dim,fontFamily:mono}},'—'));",
    "audio_anchor": "make it faster",
    "anchor_mode": "through",
    "source_headline": "The broad request."
  },
  {
    "framesFrom": 790,
    "framesTo": 1316,
    "code": "const NARRATION_TEXT = \"The biggest usage killer isn't a setting. <pause 0.3s> It's a feeling. The feeling that you're almost there, so you try one more time. It starts with a broad request. <pause 0.3s> \\\"The app feels slow \\u2014 can you make it faster?\\\" No file, no function, no symptom. Just a vibe, handed to a tool that can only act on specifics. With nothing specific to go on, Claude does the only thing it can. <pause 0.3s> It reads everything. Thirty files, just to guess where slow lives. A vague question forces a giant search, and that whole search lands in your context. Its guess is close, but not quite right. <pause 0.3s> So you say \\\"no, not that \\u2014 try again.\\\" And the whole loop runs a second time. Each retry doesn't start clean. <pause 0.3s> It re-reads every file, every failed attempt, every back-and-forth before it. The spiral gets wider each turn. Five retries on a broad request can cost more than fifty precise ones. <pause 0.6s> And it feels productive the entire time, because something is always happening on screen. That's the cruelest part of the spiral. It looks exactly like progress, right up until the limit hits. That's the trap. <pause 0.3s> Vague plus retry is the fastest way there is to drain a window. <pause 0.4s> So what does the opposite look like? Two developers, same task \\u2014 let's run them side by side.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nreturn React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,display:'flex',alignItems:'center',justifyContent:'center',fontFamily:D.font_display}},React.createElement(Kit.DotGrid,{}),React.createElement(Kit.Battery,{rem:0.6}),React.createElement('div',{style:{fontSize:R(w*0.02),color:D.text_dim,fontFamily:mono}},'—'));",
    "audio_anchor": "reads everything",
    "anchor_mode": "land",
    "source_headline": "It reads everything."
  },
  {
    "framesFrom": 1316,
    "framesTo": 1630,
    "code": "const NARRATION_TEXT = \"The biggest usage killer isn't a setting. <pause 0.3s> It's a feeling. The feeling that you're almost there, so you try one more time. It starts with a broad request. <pause 0.3s> \\\"The app feels slow \\u2014 can you make it faster?\\\" No file, no function, no symptom. Just a vibe, handed to a tool that can only act on specifics. With nothing specific to go on, Claude does the only thing it can. <pause 0.3s> It reads everything. Thirty files, just to guess where slow lives. A vague question forces a giant search, and that whole search lands in your context. Its guess is close, but not quite right. <pause 0.3s> So you say \\\"no, not that \\u2014 try again.\\\" And the whole loop runs a second time. Each retry doesn't start clean. <pause 0.3s> It re-reads every file, every failed attempt, every back-and-forth before it. The spiral gets wider each turn. Five retries on a broad request can cost more than fifty precise ones. <pause 0.6s> And it feels productive the entire time, because something is always happening on screen. That's the cruelest part of the spiral. It looks exactly like progress, right up until the limit hits. That's the trap. <pause 0.3s> Vague plus retry is the fastest way there is to drain a window. <pause 0.4s> So what does the opposite look like? Two developers, same task \\u2014 let's run them side by side.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nreturn React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,display:'flex',alignItems:'center',justifyContent:'center',fontFamily:D.font_display}},React.createElement(Kit.DotGrid,{}),React.createElement(Kit.Battery,{rem:0.6}),React.createElement('div',{style:{fontSize:R(w*0.02),color:D.text_dim,fontFamily:mono}},'—'));",
    "audio_anchor": "a second time",
    "anchor_mode": "through",
    "source_headline": "Try again."
  },
  {
    "framesFrom": 1630,
    "framesTo": 1795,
    "code": "const NARRATION_TEXT = \"The biggest usage killer isn't a setting. <pause 0.3s> It's a feeling. The feeling that you're almost there, so you try one more time. It starts with a broad request. <pause 0.3s> \\\"The app feels slow \\u2014 can you make it faster?\\\" No file, no function, no symptom. Just a vibe, handed to a tool that can only act on specifics. With nothing specific to go on, Claude does the only thing it can. <pause 0.3s> It reads everything. Thirty files, just to guess where slow lives. A vague question forces a giant search, and that whole search lands in your context. Its guess is close, but not quite right. <pause 0.3s> So you say \\\"no, not that \\u2014 try again.\\\" And the whole loop runs a second time. Each retry doesn't start clean. <pause 0.3s> It re-reads every file, every failed attempt, every back-and-forth before it. The spiral gets wider each turn. Five retries on a broad request can cost more than fifty precise ones. <pause 0.6s> And it feels productive the entire time, because something is always happening on screen. That's the cruelest part of the spiral. It looks exactly like progress, right up until the limit hits. That's the trap. <pause 0.3s> Vague plus retry is the fastest way there is to drain a window. <pause 0.4s> So what does the opposite look like? Two developers, same task \\u2014 let's run them side by side.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nreturn React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,display:'flex',alignItems:'center',justifyContent:'center',fontFamily:D.font_display}},React.createElement(Kit.DotGrid,{}),React.createElement(Kit.Battery,{rem:0.6}),React.createElement('div',{style:{fontSize:R(w*0.02),color:D.text_dim,fontFamily:mono}},'—'));",
    "audio_anchor": "gets wider",
    "anchor_mode": "through",
    "source_headline": "Wider every turn."
  },
  {
    "framesFrom": 1795,
    "framesTo": 2499,
    "code": "const NARRATION_TEXT = \"The biggest usage killer isn't a setting. <pause 0.3s> It's a feeling. The feeling that you're almost there, so you try one more time. It starts with a broad request. <pause 0.3s> \\\"The app feels slow \\u2014 can you make it faster?\\\" No file, no function, no symptom. Just a vibe, handed to a tool that can only act on specifics. With nothing specific to go on, Claude does the only thing it can. <pause 0.3s> It reads everything. Thirty files, just to guess where slow lives. A vague question forces a giant search, and that whole search lands in your context. Its guess is close, but not quite right. <pause 0.3s> So you say \\\"no, not that \\u2014 try again.\\\" And the whole loop runs a second time. Each retry doesn't start clean. <pause 0.3s> It re-reads every file, every failed attempt, every back-and-forth before it. The spiral gets wider each turn. Five retries on a broad request can cost more than fifty precise ones. <pause 0.6s> And it feels productive the entire time, because something is always happening on screen. That's the cruelest part of the spiral. It looks exactly like progress, right up until the limit hits. That's the trap. <pause 0.3s> Vague plus retry is the fastest way there is to drain a window. <pause 0.4s> So what does the opposite look like? Two developers, same task \\u2014 let's run them side by side.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nreturn React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,display:'flex',alignItems:'center',justifyContent:'center',fontFamily:D.font_display}},React.createElement(Kit.DotGrid,{}),React.createElement(Kit.Battery,{rem:0.6}),React.createElement('div',{style:{fontSize:R(w*0.02),color:D.text_dim,fontFamily:mono}},'—'));",
    "audio_anchor": "precise ones",
    "anchor_mode": "land",
    "source_headline": "Five vague, fifty precise."
  },
  {
    "framesFrom": 2499,
    "framesTo": 2597,
    "code": "const NARRATION_TEXT = \"The biggest usage killer isn't a setting. <pause 0.3s> It's a feeling. The feeling that you're almost there, so you try one more time. It starts with a broad request. <pause 0.3s> \\\"The app feels slow \\u2014 can you make it faster?\\\" No file, no function, no symptom. Just a vibe, handed to a tool that can only act on specifics. With nothing specific to go on, Claude does the only thing it can. <pause 0.3s> It reads everything. Thirty files, just to guess where slow lives. A vague question forces a giant search, and that whole search lands in your context. Its guess is close, but not quite right. <pause 0.3s> So you say \\\"no, not that \\u2014 try again.\\\" And the whole loop runs a second time. Each retry doesn't start clean. <pause 0.3s> It re-reads every file, every failed attempt, every back-and-forth before it. The spiral gets wider each turn. Five retries on a broad request can cost more than fifty precise ones. <pause 0.6s> And it feels productive the entire time, because something is always happening on screen. That's the cruelest part of the spiral. It looks exactly like progress, right up until the limit hits. That's the trap. <pause 0.3s> Vague plus retry is the fastest way there is to drain a window. <pause 0.4s> So what does the opposite look like? Two developers, same task \\u2014 let's run them side by side.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nreturn React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,display:'flex',alignItems:'center',justifyContent:'center',fontFamily:D.font_display}},React.createElement(Kit.DotGrid,{}),React.createElement(Kit.Battery,{rem:0.6}),React.createElement('div',{style:{fontSize:R(w*0.02),color:D.text_dim,fontFamily:mono}},'—'));",
    "audio_anchor": "same task",
    "anchor_mode": "through",
    "source_headline": "Same task, the other way."
  }
]