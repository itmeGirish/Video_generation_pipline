[
  {
    "framesFrom": 0,
    "framesTo": 462,
    "code": "const NARRATION_TEXT = \"Everything Claude re-reads lives in one place. <pause 0.3s> The context window. Think of it as a backpack you re-carry on every single turn. Two habits fill that backpack faster than anything else. <pause 0.3s> The first is the long-running chat. A fresh question at message two is cheap. <pause 0.3s> The same question at message twenty-five drags twenty-four messages of history behind it. The second habit is the giant paste. <pause 0.3s> You drop a two-thousand-line file in just to ask about one function. And the cruel part isn't that it costs you once \\u2014 it sits in the backpack and gets re-carried on every turn that follows. Now here's the part that catches everyone off guard. <pause 0.4s> The most expensive thing in that backpack isn't even your code. It's the stuff you forgot was in there \\u2014 that you never packed yourself. <pause 0.4s> Your project's instructions file gets re-read on every single request. Every tool you've ever connected loads its full manual the moment the session starts. You're paying for all of it, on every turn, and you never typed a word of it. So long chats and big pastes quietly inflate every turn that comes after them. <pause 0.4s> But neither one is the number-one way people destroy their usage without noticing. And that one actually feels productive while it's happening.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nreturn React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,display:'flex',alignItems:'center',justifyContent:'center',fontFamily:D.font_display}},React.createElement(Kit.DotGrid,{}),React.createElement(Kit.Battery,{rem:0.6}),React.createElement('div',{style:{fontSize:R(w*0.02),color:D.text_dim,fontFamily:mono}},'—'));",
    "audio_anchor": "context window",
    "anchor_mode": "appear",
    "source_headline": "The container."
  },
  {
    "framesFrom": 462,
    "framesTo": 665,
    "code": "const NARRATION_TEXT = \"Everything Claude re-reads lives in one place. <pause 0.3s> The context window. Think of it as a backpack you re-carry on every single turn. Two habits fill that backpack faster than anything else. <pause 0.3s> The first is the long-running chat. A fresh question at message two is cheap. <pause 0.3s> The same question at message twenty-five drags twenty-four messages of history behind it. The second habit is the giant paste. <pause 0.3s> You drop a two-thousand-line file in just to ask about one function. And the cruel part isn't that it costs you once \\u2014 it sits in the backpack and gets re-carried on every turn that follows. Now here's the part that catches everyone off guard. <pause 0.4s> The most expensive thing in that backpack isn't even your code. It's the stuff you forgot was in there \\u2014 that you never packed yourself. <pause 0.4s> Your project's instructions file gets re-read on every single request. Every tool you've ever connected loads its full manual the moment the session starts. You're paying for all of it, on every turn, and you never typed a word of it. So long chats and big pastes quietly inflate every turn that comes after them. <pause 0.4s> But neither one is the number-one way people destroy their usage without noticing. And that one actually feels productive while it's happening.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nreturn React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,display:'flex',alignItems:'center',justifyContent:'center',fontFamily:D.font_display}},React.createElement(Kit.DotGrid,{}),React.createElement(Kit.Battery,{rem:0.6}),React.createElement('div',{style:{fontSize:R(w*0.02),color:D.text_dim,fontFamily:mono}},'—'));",
    "audio_anchor": "long-running chat",
    "anchor_mode": "through",
    "source_headline": "The long chat."
  },
  {
    "framesFrom": 665,
    "framesTo": 833,
    "code": "const NARRATION_TEXT = \"Everything Claude re-reads lives in one place. <pause 0.3s> The context window. Think of it as a backpack you re-carry on every single turn. Two habits fill that backpack faster than anything else. <pause 0.3s> The first is the long-running chat. A fresh question at message two is cheap. <pause 0.3s> The same question at message twenty-five drags twenty-four messages of history behind it. The second habit is the giant paste. <pause 0.3s> You drop a two-thousand-line file in just to ask about one function. And the cruel part isn't that it costs you once \\u2014 it sits in the backpack and gets re-carried on every turn that follows. Now here's the part that catches everyone off guard. <pause 0.4s> The most expensive thing in that backpack isn't even your code. It's the stuff you forgot was in there \\u2014 that you never packed yourself. <pause 0.4s> Your project's instructions file gets re-read on every single request. Every tool you've ever connected loads its full manual the moment the session starts. You're paying for all of it, on every turn, and you never typed a word of it. So long chats and big pastes quietly inflate every turn that comes after them. <pause 0.4s> But neither one is the number-one way people destroy their usage without noticing. And that one actually feels productive while it's happening.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nreturn React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,display:'flex',alignItems:'center',justifyContent:'center',fontFamily:D.font_display}},React.createElement(Kit.DotGrid,{}),React.createElement(Kit.Battery,{rem:0.6}),React.createElement('div',{style:{fontSize:R(w*0.02),color:D.text_dim,fontFamily:mono}},'—'));",
    "audio_anchor": "message twenty-five",
    "anchor_mode": "land",
    "source_headline": "Message twenty-five."
  },
  {
    "framesFrom": 833,
    "framesTo": 1279,
    "code": "const NARRATION_TEXT = \"Everything Claude re-reads lives in one place. <pause 0.3s> The context window. Think of it as a backpack you re-carry on every single turn. Two habits fill that backpack faster than anything else. <pause 0.3s> The first is the long-running chat. A fresh question at message two is cheap. <pause 0.3s> The same question at message twenty-five drags twenty-four messages of history behind it. The second habit is the giant paste. <pause 0.3s> You drop a two-thousand-line file in just to ask about one function. And the cruel part isn't that it costs you once \\u2014 it sits in the backpack and gets re-carried on every turn that follows. Now here's the part that catches everyone off guard. <pause 0.4s> The most expensive thing in that backpack isn't even your code. It's the stuff you forgot was in there \\u2014 that you never packed yourself. <pause 0.4s> Your project's instructions file gets re-read on every single request. Every tool you've ever connected loads its full manual the moment the session starts. You're paying for all of it, on every turn, and you never typed a word of it. So long chats and big pastes quietly inflate every turn that comes after them. <pause 0.4s> But neither one is the number-one way people destroy their usage without noticing. And that one actually feels productive while it's happening.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nreturn React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,display:'flex',alignItems:'center',justifyContent:'center',fontFamily:D.font_display}},React.createElement(Kit.DotGrid,{}),React.createElement(Kit.Battery,{rem:0.6}),React.createElement('div',{style:{fontSize:R(w*0.02),color:D.text_dim,fontFamily:mono}},'—'));",
    "audio_anchor": "giant paste",
    "anchor_mode": "land",
    "source_headline": "The giant paste."
  },
  {
    "framesFrom": 1279,
    "framesTo": 2282,
    "code": "const NARRATION_TEXT = \"Everything Claude re-reads lives in one place. <pause 0.3s> The context window. Think of it as a backpack you re-carry on every single turn. Two habits fill that backpack faster than anything else. <pause 0.3s> The first is the long-running chat. A fresh question at message two is cheap. <pause 0.3s> The same question at message twenty-five drags twenty-four messages of history behind it. The second habit is the giant paste. <pause 0.3s> You drop a two-thousand-line file in just to ask about one function. And the cruel part isn't that it costs you once \\u2014 it sits in the backpack and gets re-carried on every turn that follows. Now here's the part that catches everyone off guard. <pause 0.4s> The most expensive thing in that backpack isn't even your code. It's the stuff you forgot was in there \\u2014 that you never packed yourself. <pause 0.4s> Your project's instructions file gets re-read on every single request. Every tool you've ever connected loads its full manual the moment the session starts. You're paying for all of it, on every turn, and you never typed a word of it. So long chats and big pastes quietly inflate every turn that comes after them. <pause 0.4s> But neither one is the number-one way people destroy their usage without noticing. And that one actually feels productive while it's happening.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nreturn React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,display:'flex',alignItems:'center',justifyContent:'center',fontFamily:D.font_display}},React.createElement(Kit.DotGrid,{}),React.createElement(Kit.Battery,{rem:0.6}),React.createElement('div',{style:{fontSize:R(w*0.02),color:D.text_dim,fontFamily:mono}},'—'));",
    "audio_anchor": "catches everyone off guard",
    "anchor_mode": "land",
    "source_headline": "You never packed half of it."
  },
  {
    "framesFrom": 2282,
    "framesTo": 2482,
    "code": "const NARRATION_TEXT = \"Everything Claude re-reads lives in one place. <pause 0.3s> The context window. Think of it as a backpack you re-carry on every single turn. Two habits fill that backpack faster than anything else. <pause 0.3s> The first is the long-running chat. A fresh question at message two is cheap. <pause 0.3s> The same question at message twenty-five drags twenty-four messages of history behind it. The second habit is the giant paste. <pause 0.3s> You drop a two-thousand-line file in just to ask about one function. And the cruel part isn't that it costs you once \\u2014 it sits in the backpack and gets re-carried on every turn that follows. Now here's the part that catches everyone off guard. <pause 0.4s> The most expensive thing in that backpack isn't even your code. It's the stuff you forgot was in there \\u2014 that you never packed yourself. <pause 0.4s> Your project's instructions file gets re-read on every single request. Every tool you've ever connected loads its full manual the moment the session starts. You're paying for all of it, on every turn, and you never typed a word of it. So long chats and big pastes quietly inflate every turn that comes after them. <pause 0.4s> But neither one is the number-one way people destroy their usage without noticing. And that one actually feels productive while it's happening.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nreturn React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,display:'flex',alignItems:'center',justifyContent:'center',fontFamily:D.font_display}},React.createElement(Kit.DotGrid,{}),React.createElement(Kit.Battery,{rem:0.6}),React.createElement('div',{style:{fontSize:R(w*0.02),color:D.text_dim,fontFamily:mono}},'—'));",
    "audio_anchor": "number-one way",
    "anchor_mode": "land",
    "source_headline": "The worst one is next."
  }
]