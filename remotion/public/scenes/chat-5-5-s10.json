[
  {
    "framesFrom": 0,
    "framesTo": 6297,
    "code": "const e=React.createElement;\nconst s=t=>Math.round(t*fps);\nconst cl={extrapolateLeft:'clamp',extrapolateRight:'clamp'};\nconst CH='ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789 ';\nconst M=18;\nconst rows=[\n['DOING THINGS','GPT-5.5',D.violet,0,s(4),s(1)],\n['THINKING','CLAUDE 4.7',D.cyan,0,s(5.5),s(1)],\n['HONESTY','CLAUDE 4.7',D.green,1,s(7),s(1)],\n['ETHICS','GPT-5.5',D.green,2,s(9),s(1.7)],\n['VALUE','BUILD THE PIPELINE',D.amber,3,s(12.5),s(1)]\n];\nconst bW=width*0.86,bH=height*0.65,bX=(width-bW)/2,bY=(height-bH)/2;\nconst rH=50,gap=12,cW=Math.floor(bW*0.4/M);\nconst bOp=interpolate(frame,[0,Math.max(1,s(1.5))],[0,1],cl);\nconst bSc=interpolate(frame,[0,Math.max(1,s(2))],[0.96,1],cl);\nconst getC=(fin,ci,el,dur,su)=>{\nif(el<0)return ' ';\nif(!su){\nif(ci>=fin.length)return ' ';\nif(el>=(ci+1)*(dur/fin.length))return fin[ci];\nreturn CH[((el*7+ci*13)|0)%CH.length];}\nconst fk='CLAUDE 4.7';\nif(el<dur*0.45)return CH[((el*7+ci*13)|0)%CH.length];\nif(el<dur*0.72){\nif(ci>=fk.length)return ' ';\nif((el-dur*0.45)/(dur*0.27)>=(ci+1)/fk.length)return fk[ci];\nreturn CH[((el*7+ci*13)|0)%CH.length];}\nif(ci>=fin.length)return ' ';\nif((el-dur*0.72)/(dur*0.28)>=(ci+1)/fin.length)return fin[ci];\nreturn CH[((el*11+ci*17)|0)%CH.length];\n};\nconst mkChk=(typ,sf,dur)=>{\nif(frame<sf+dur)return '';\nconst af=frame-sf-dur;\nif(typ===2)return af<s(0.5)?(af%Math.max(1,s(0.2))<s(0.1)?'?':''):'✓';\nif(typ===1)return '✓✓';\nif(typ===3)return '↻';\nreturn '✓';\n};\nconst rEls=rows.map(([cat,res,col,typ,sf,dur],ri)=>{\nconst el=frame-sf,done=el>=dur,ry=bY+62+ri*(rH+gap);\nconst op=interpolate(frame,[Math.max(0,sf-s(0.5)),Math.max(sf-s(0.5)+1,sf)],[0.3,1],cl);\nconst chars=Array.from({length:M},(_,ci)=>{\nconst ch=el>0?getC(res,ci,el,dur,typ===2):' ';\nreturn e('div',{key:ci,style:{display:'inline-block',width:cW,height:rH-6,lineHeight:(rH-6)+'px',background:D.surface,margin:'0 1px',textAlign:'center',fontFamily:D.font_mono,fontSize:20,fontWeight:'bold',color:done?col:D.text,position:'relative',borderRadius:2}},ch,e('div',{style:{position:'absolute',left:0,right:0,top:'49%',height:1,background:D.text_dim,opacity:0.3}}));});\nconst chkOp=done?Math.min(1,spring({frame:Math.max(0,frame-sf-dur),fps,config:{damping:12,stiffness:200}})):0;\nreturn e('div',{key:ri,style:{position:'absolute',left:bX,top:ry,width:bW,height:rH,opacity:op}},\ne('div',{style:{position:'absolute',left:16,top:0,lineHeight:rH+'px',fontFamily:D.font_mono,fontSize:16,color:D.text_dim,letterSpacing:2}},cat),\ne('div',{style:{position:'absolute',left:bW*0.44,top:4,display:'flex'}},...chars),\ne('div',{style:{position:'absolute',right:16,top:0,lineHeight:rH+'px',fontFamily:D.font_mono,fontSize:20,color:col,opacity:chkOp}},mkChk(typ,sf,dur)));\n});\nreturn e('div',{style:{position:'absolute',inset:0,background:D.bg}},\ne('div',{style:{position:'absolute',inset:0,opacity:D.dot_grid_opacity*0.3,backgroundImage:'radial-gradient(circle,'+D.text_dim+' 1px,transparent 1px)',backgroundSize:D.dot_grid_spacing+'px '+D.dot_grid_spacing+'px'}}),\ne('div',{style:{position:'absolute',left:bX,top:bY,width:bW,height:bH,background:D.surface,borderRadius:8,border:'1px solid '+D.text_dim,opacity:bOp,transform:'scale('+bSc+')',transformOrigin:'center',boxShadow:'0 10px 50px rgba(0,0,0,0.7)'}},\ne('div',{style:{padding:'14px 24px',borderBottom:'1px solid '+D.text_dim,fontFamily:D.font_display,fontSize:20,color:D.amber,letterSpacing:6}},'● FINAL VERDICT')),\n...rEls);",
    "audio_anchor": "on the board",
    "source_headline": "SPLIT-FLAP DEPARTURE BOARD materializes — Solari board aesthetic.."
  },
  {
    "framesFrom": 6297,
    "framesTo": 7600,
    "code": "const e = React.createElement;\nconst sf = (a, b) => Math.max(a + 1, b);\nconst cw = 380;\nconst ch = 240;\nconst pad = 50;\nconst d1 = 4;\nconst d2 = Math.floor(durationInFrames * 0.42);\nconst iSpr = spring({ frame: frame - d1, fps, config: { damping: 11, stiffness: 190 } });\nconst iOp = interpolate(frame, [d1, sf(d1, d1 + 6)], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\nconst rSpr = spring({ frame: frame - d2, fps, config: { damping: 11, stiffness: 190 } });\nconst rOp = interpolate(frame, [d2, sf(d2, d2 + 6)], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\nconst gG = 0.4 + 0.6 * Math.abs(Math.sin(frame * 0.09));\nconst aG = 0.4 + 0.6 * Math.abs(Math.sin(frame * 0.09 + 1.5));\nconst iFl = interpolate(frame, [d1, sf(d1, d1 + 2), sf(d1 + 2, d1 + 7)], [0, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\nconst rFl = interpolate(frame, [d2, sf(d2, d2 + 2), sf(d2 + 2, d2 + 7)], [0, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\nconst mkCard = (x, y, op, sc, col, glow, fl, kids) =>\n  e('div', { style: {\n    position: 'absolute', left: x, top: y, width: cw, height: ch,\n    opacity: op, transform: 'scale(' + sc + ')', transformOrigin: 'center',\n    borderRadius: 16, background: D.surface, border: '2px solid ' + col,\n    boxShadow: '0 0 ' + (30 * glow) + 'px ' + (10 * glow) + 'px ' + col,\n    overflow: 'hidden',\n  }},\n    e('div', { style: { position: 'absolute', inset: 0, background: col, opacity: fl * 0.4, pointerEvents: 'none' } }),\n    ...kids\n  );\nconst iceDraw = e('div', { style: { position: 'absolute', left: 20, top: 20, width: cw * 0.32, height: ch - 40 } },\n  e('div', { style: { position: 'absolute', top: '35%', left: 0, right: 0, height: 2, background: D.cyan, opacity: 0.4 } }),\n  e('div', { style: { position: 'absolute', top: '12%', left: '50%', transform: 'translateX(-50%)', width: 0, height: 0, borderLeft: '16px solid transparent', borderRight: '16px solid transparent', borderBottom: '28px solid ' + D.cyan } }),\n  e('div', { style: { position: 'absolute', top: '38%', left: '10%', width: '80%', height: '55%', background: D.cyan, opacity: 0.2, clipPath: 'polygon(15% 0%, 85% 0%, 100% 100%, 0% 100%)' } })\n);\nconst iceText = e('div', { style: { position: 'absolute', right: 18, top: 28, width: cw * 0.52 } },\n  e('div', { style: { fontFamily: D.font_display, fontSize: 22, fontWeight: 800, color: D.green, lineHeight: 1.2, textTransform: 'uppercase', letterSpacing: 2, textShadow: '0 0 ' + (12 * gG) + 'px ' + D.green } }, 'MYTHOS IS'),\n  e('div', { style: { fontFamily: D.font_display, fontSize: 22, fontWeight: 800, color: D.green, lineHeight: 1.2, textTransform: 'uppercase', letterSpacing: 2, textShadow: '0 0 ' + (12 * gG) + 'px ' + D.green, marginTop: 4 } }, 'WAITING'),\n  e('div', { style: { fontFamily: D.font_mono, fontSize: 11, color: D.text_dim, marginTop: 12, opacity: 0.6 } }, 'RESTRICTED \\u2022 8 COMPANIES')\n);\nconst ballProg = interpolate(frame, [d2, sf(d2, d2 + Math.floor(durationInFrames * 0.5))], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\nconst bx = interpolate(ballProg, [0, 0.33, 0.66, 1], [15, 55, 10, 50], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\nconst by = interpolate(ballProg, [0, 0.33, 0.66, 1], [8, 38, 62, 88], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\nconst ramps = [\n  { t: '12%', l: '5%', w: 55, r: 15 },\n  { t: '38%', l: '35%', w: 50, r: -18 },\n  { t: '60%', l: '0%', w: 60, r: 12 },\n  { t: '82%', l: '25%', w: 45, r: 0 },\n];\nconst rubeDraw = e('div', { style: { position: 'absolute', left: 20, top: 20, width: cw * 0.32, height: ch - 40 } },\n  ...ramps.map((p, i) =>\n    e('div', { key: 'r' + i, style: { position: 'absolute', top: p.t, left: p.l, width: p.w, height: 3, background: D.amber, transform: 'rotate(' + p.r + 'deg)', borderRadius: 2 } })\n  ),\n  e('div', { key: 'ball', style: { position: 'absolute', top: by + '%', left: bx + '%', width: 8, height: 8, borderRadius: '50%', background: D.white, boxShadow: '0 0 6px ' + D.amber } }),\n  e('div', { key: 'g1', style: { position: 'absolute', top: '26%', left: '0%', width: 16, height: 16, border: '2px solid ' + D.amber, borderRadius: '50%', transform: 'rotate(' + (frame * 3) + 'deg)' } },\n    e('div', { style: { position: 'absolute', top: '50%', left: -2, right: -2, height: 2, background: D.amber, transform: 'translateY(-50%)' } })\n  ),\n  e('div', { key: 'g2', style: { position: 'absolute', top: '52%', right: '5%', width: 14, height: 14, border: '2px solid ' + D.amber, borderRadius: '50%', transform: 'rotate(' + (-frame * 4) + 'deg)' } },\n    e('div', { style: { position: 'absolute', left: '50%', top: -2, bottom: -2, width: 2, background: D.amber, transform: 'translateX(-50%)' } })\n  )\n);\nconst rubeText = e('div', { style: { position: 'absolute', right: 18, top: 28, width: cw * 0.52 } },\n  e('div', { style: { fontFamily: D.font_display, fontSize: 22, fontWeight: 800, color: D.amber, lineHeight: 1.2, textTransform: 'uppercase', letterSpacing: 2, textShadow: '0 0 ' + (12 * aG) + 'px ' + D.amber } }, 'BUILD THIS'),\n  e('div', { style: { fontFamily: D.font_display, fontSize: 22, fontWeight: 800, color: D.amber, lineHeight: 1.2, textTransform: 'uppercase', letterSpacing: 2, textShadow: '0 0 ' + (12 * aG) + 'px ' + D.amber, marginTop: 4 } }, 'MONDAY'),\n  e('div', { style: { fontFamily: D.font_mono, fontSize: 11, color: D.text_dim, marginTop: 12, opacity: 0.6 } }, 'PIPELINE \\u2022 SHIP IT')\n);\nreturn e('div', { style: { position: 'absolute', inset: 0, background: D.bg } },\n  mkCard(pad, height - ch - pad, iOp, iSpr, D.green, gG, iFl, [iceDraw, iceText]),\n  mkCard(width - cw - pad, height - ch - pad, rOp, rSpr, D.amber, aG, rFl, [rubeDraw, rubeText])\n);",
    "audio_anchor": "Mythos goes public",
    "source_headline": "Callbacks flash in corners.."
  },
  {
    "framesFrom": 7600,
    "framesTo": 8306,
    "code": "const h = React.createElement;\nconst ALPHA = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789';\nconst prand = (s) => { const x = Math.sin(s * 127.1 + 311.7) * 43758.5453; return x - Math.floor(x); };\nconst dur = durationInFrames;\nconst scrambleEnd = Math.floor(dur * 0.26);\nconst resolveStart = Math.floor(dur * 0.26);\nconst resolveSpan = Math.floor(dur * 0.22);\nconst borderStart = Math.floor(dur * 0.5);\nconst borderDur = Math.floor(dur * 0.14);\nconst taglineIn = Math.floor(dur * 0.6);\nconst promptIn = Math.floor(dur * 0.74);\nconst heroStr = 'SUBSCRIBE';\nconst COLS = 15;\nconst ROWS = 5;\nconst HROW = 2;\nconst hOff = Math.floor((COLS - heroStr.length) / 2);\nconst cW = Math.min(Math.floor(width / (COLS + 3)), 68);\nconst cH = Math.floor(cW * 1.25);\nconst gX = 5;\nconst gY = 6;\nconst bW = COLS * (cW + gX) - gX;\nconst bH = ROWS * (cH + gY) - gY;\nconst bL = Math.floor((width - bW) / 2);\nconst bT = Math.floor(height * 0.12);\nconst cells = [];\nfor (let r = 0; r < ROWS; r++) {\n  for (let c = 0; c < COLS; c++) {\n    const id = r * COLS + c;\n    const tick = Math.floor(frame / 2);\n    const rc = ALPHA[Math.floor(prand(tick + id * 17) * ALPHA.length)];\n    const isHero = r === HROW;\n    const hi = c - hOff;\n    const inHeroText = isHero && hi >= 0 && hi < heroStr.length;\n    const sf = inHeroText ? resolveStart + Math.floor(hi * resolveSpan / heroStr.length) : 0;\n    const settled = inHeroText && frame >= sf;\n    const ch = isHero ? (inHeroText ? (settled ? heroStr[hi] : rc) : (frame < scrambleEnd ? rc : ' ')) : rc;\n    const col = settled ? D.cyan : D.text;\n    const op = isHero ? 1 : interpolate(frame, [scrambleEnd, Math.max(scrambleEnd + 1, scrambleEnd + Math.floor(dur * 0.1))], [1, 0.08], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\n    cells.push(h('div', { key: id, style: { position: 'absolute', left: bL + c * (cW + gX), top: bT + r * (cH + gY), width: cW, height: cH, background: 'linear-gradient(to bottom, ' + D.surface + ' 47%, ' + D.bg + ' 47%, ' + D.bg + ' 53%, ' + D.surface + ' 53%)', borderRadius: 3, display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: D.font_mono, fontSize: cH * 0.6, fontWeight: 'bold', color: col, opacity: op } }, ch));\n  }\n}\nconst bp = interpolate(frame, [borderStart, Math.max(borderStart + 1, borderStart + borderDur)], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\nconst hL = bL - 10;\nconst hT2 = bT + HROW * (cH + gY) - 10;\nconst hW = bW + 20;\nconst hH = cH + 20;\nconst perim = 2 * (hW + hH);\nconst drawn = bp * perim;\nconst th = 3;\nconst mkB = (key, l, t, w, ht) => h('div', { key: key, style: { position: 'absolute', left: l, top: t, width: w, height: ht, background: D.cyan, boxShadow: '0 0 14px ' + D.cyan, borderRadius: 1 } });\nconst tL = Math.min(Math.max(drawn, 0), hW);\nconst rL = Math.min(Math.max(drawn - hW, 0), hH);\nconst btL = Math.min(Math.max(drawn - hW - hH, 0), hW);\nconst lL = Math.min(Math.max(drawn - 2 * hW - hH, 0), hH);\nconst borders = frame >= borderStart ? [mkB('bt', hL, hT2, tL, th), mkB('br', hL + hW - th, hT2, th, rL), mkB('bb', hL + hW - btL, hT2 + hH - th, btL, th), mkB('bl', hL, hT2 + hH - lL, th, lL)] : [];\nconst tOp = interpolate(frame, [taglineIn, Math.max(taglineIn + 1, taglineIn + Math.floor(dur * 0.07))], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\nconst tagline = h('div', { style: { position: 'absolute', top: bT + bH + 55, left: 0, right: 0, textAlign: 'center', fontFamily: D.font_display, fontSize: Math.floor(height * 0.035), color: D.text_dim, opacity: tOp, letterSpacing: 2 } }, 'Real benchmarks. Real code. Real money.');\nconst pOp = interpolate(frame, [promptIn, Math.max(promptIn + 1, promptIn + Math.floor(dur * 0.05))], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\nconst youBlink = frame >= promptIn ? (Math.floor((frame - promptIn) / 10) % 2 === 0 ? 1 : 0.15) : 0;\nconst prompt = h('div', { style: { position: 'absolute', top: bT + bH + 110, left: 0, right: 0, textAlign: 'center', fontFamily: D.font_mono, fontSize: Math.floor(height * 0.03), color: D.text, opacity: pOp } }, h('span', null, 'PLATFORM OR PRECISION? \\u2014 '), h('span', { style: { color: D.amber, opacity: youBlink, fontWeight: 'bold' } }, 'YOU'));\nconst backdrop = h('div', { style: { position: 'absolute', left: bL - 24, top: bT - 24, width: bW + 48, height: bH + 48, background: D.bg, borderRadius: 12, border: '1px solid ' + D.surface } });\nconst dotGrid = h('div', { style: { position: 'absolute', inset: 0, backgroundImage: 'radial-gradient(circle, ' + D.text_dim + ' 1px, transparent 1px)', backgroundSize: D.dot_grid_spacing + 'px ' + D.dot_grid_spacing + 'px', opacity: D.dot_grid_opacity } });\nreturn h('div', { style: { position: 'absolute', inset: 0, background: D.bg, overflow: 'hidden' } }, dotGrid, backdrop, ...cells, ...borders, tagline, prompt);",
    "audio_anchor": "hit subscribe",
    "source_headline": "Departure board begins SCRAMBLING — all rows flip to random letters simultaneously.."
  },
  {
    "framesFrom": 8306,
    "framesTo": 8863,
    "code": "const e = React.createElement;\nconst dur = durationInFrames;\nconst cx = width * 0.5;\nconst cy = height * 0.5;\nconst clamp = {extrapolateLeft:'clamp',extrapolateRight:'clamp'};\nconst si = (a, b, oA, oB) => interpolate(frame, [a, Math.max(a+1,b)], [oA,oB], clamp);\nconst CLEAR = Math.floor(dur * 0.08);\nconst Q_IN = Math.floor(dur * 0.10);\nconst Q_PEAK = Math.floor(dur * 0.20);\nconst Q_FADE = Math.floor(dur * 0.30);\nconst B_IN = Math.floor(dur * 0.33);\nconst LM_START = Math.floor(dur * 0.37);\nconst LM_END = Math.floor(dur * 0.52);\nconst N_LOCK = Math.floor(dur * 0.55);\nconst R2_IN = Math.floor(dur * 0.57);\nconst R2_LOCK = Math.floor(dur * 0.71);\nconst BLACK = Math.floor(dur * 0.875);\nif (frame >= BLACK) return e(AbsoluteFill, {style:{backgroundColor:D.bg}});\nconst Flap = (ch, sz, col, glow) => e('div', {style:{display:'inline-flex',alignItems:'center',justifyContent:'center',width:sz*0.75,height:sz*1.12,background:D.surface,borderRadius:5,margin:'0 3px',position:'relative',overflow:'hidden',boxShadow:glow?('0 0 16px '+col):'none'}}, e('span',{style:{fontFamily:D.font_mono,fontSize:sz*0.68,fontWeight:900,color:col,zIndex:2,position:'relative'}},ch), e('div',{style:{position:'absolute',left:0,right:0,top:'49%',height:2,background:D.bg,opacity:0.6,zIndex:3}}));\nconst clearEls = [];\nif (frame <= CLEAR + 8) {\n  for (let i = 0; i < 6; i++) {\n    const rs = Math.floor(i * CLEAR / 6);\n    const re = rs + Math.max(1, Math.floor(CLEAR / 6));\n    const p = si(rs, re, 1, 0);\n    if (p > 0) clearEls.push(e('div',{key:'c'+i,style:{position:'absolute',left:width*0.12,right:width*0.12,top:height*0.15+i*72,height:50,background:D.surface,borderRadius:4,transform:'scaleY('+p+')',opacity:p,display:'flex',alignItems:'center',padding:'0 16px',gap:10}}, ...Array.from({length:4},(_,j)=>e('div',{key:j,style:{width:35+j*20,height:12,background:D.text_dim,borderRadius:3,opacity:0.2}}))));\n  }\n}\nlet qOp = 0;\nif (frame >= Q_IN && frame < Q_PEAK) qOp = si(Q_IN, Q_PEAK, 0, 0.45);\nelse if (frame >= Q_PEAK && frame < Q_FADE) qOp = si(Q_PEAK, Q_FADE, 0.45, 0.1);\nelse if (frame >= Q_FADE) qOp = si(Q_FADE, Q_FADE+Math.floor(dur*0.04), 0.1, 0);\nconst Q = (t,l) => qOp > 0 ? e('div',{style:{position:'absolute',top:cy-28,width:width*0.38,textAlign:'center',fontFamily:D.font_display,fontSize:24,color:D.text_dim,opacity:qOp,fontStyle:'italic',lineHeight:'1.5',...(l?{left:width*0.06}:{right:width*0.06})}},t) : null;\nconst boardOp = si(B_IN, B_IN+Math.floor(dur*0.03), 0, 1);\nconst boardY = si(B_IN, B_IN+Math.floor(dur*0.04), -30, 0);\nconst landmarks = ['1','7','3','8'];\nconst lmSegLen = Math.max(1, Math.floor((LM_END - LM_START) / landmarks.length));\nconst digs = '0123456789';\nconst numChar = (ci) => {\n  if (frame < LM_START) return ' ';\n  if (frame >= N_LOCK) return ['8','6','%'][ci];\n  if (frame < LM_END) {\n    const el = frame - LM_START;\n    const seg = Math.min(Math.floor(el / lmSegLen), 3);\n    const inS = el - seg * lmSegLen;\n    if (inS < lmSegLen * 0.65) return landmarks[seg];\n    return digs[(frame*(3+ci*5))%10];\n  }\n  return digs[(frame*(3+ci*5))%10];\n};\nconst numLocked = frame >= N_LOCK;\nconst numGlow = numLocked && frame < N_LOCK + Math.floor(dur*0.025);\nconst numCells = [0,1,2].map(i => e(React.Fragment,{key:'n'+i}, Flap(numChar(i), 88, numLocked ? D.red : D.text, numGlow)));\nconst r2 = 'smarter ≠ more honest';\nconst r2Op = si(R2_IN, R2_IN+Math.floor(dur*0.03), 0, 1);\nconst r2Locked = frame >= R2_LOCK;\nconst r2Glow = r2Locked && frame < R2_LOCK + Math.floor(dur*0.025);\nconst pool = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789';\nconst r2Chars = r2.split('').map((ch,i) => {\n  if (frame < R2_IN) return ' ';\n  const lk = R2_IN + Math.floor((R2_LOCK-R2_IN)*((i+1)/r2.length));\n  if (frame >= lk) return ch;\n  if (ch === ' ') return ' ';\n  return pool[(frame*(2+i*3))%pool.length];\n});\nconst r2Cells = r2Chars.map((ch,i) => e(React.Fragment,{key:'r'+i}, Flap(ch===' '?' ':ch, 32, r2Locked?D.amber:D.text_dim, r2Glow)));\nreturn e(AbsoluteFill, {style:{backgroundColor:D.bg}},\n  ...clearEls,\n  Q('“Like having a limb amputated.”',true),\n  Q('“Just don’t let it write your citations.”',false),\n  frame >= B_IN ? e('div',{style:{position:'absolute',left:0,right:0,top:cy-80,display:'flex',flexDirection:'column',alignItems:'center',opacity:boardOp,transform:'translateY('+boardY+'px)'}},\n    e('div',{style:{border:'2px solid '+D.text_dim,borderRadius:12,padding:'24px 36px',display:'flex',flexDirection:'column',alignItems:'center',background:D.bg}},\n      e('div',{style:{display:'flex',justifyContent:'center',marginBottom:16}}, ...numCells),\n      frame >= R2_IN ? e('div',{style:{display:'flex',justifyContent:'center',opacity:r2Op,flexWrap:'nowrap'}}, ...r2Cells) : null\n    )\n  ) : null\n);",
    "audio_anchor": "that NVIDIA engineer",
    "source_headline": "LOOP-BACK.."
  }
]