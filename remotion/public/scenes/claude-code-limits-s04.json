[
  {
    "framesFrom": 0,
    "framesTo": 673,
    "code": "const NARRATION_TEXT = \"Back to your four-word message. <pause 0.3s> \\\"Fix the login bug.\\\" To you, that's one turn. To Claude Code, it's nothing like one turn. Because Claude Code doesn't just answer. <pause 0.3s> It acts. To fix a login bug, it opens login dot t-s. Then it follows the trail into auth dot t-s, and middleware dot t-s. Three whole files, pulled into context. Then it writes a fix and runs the tests. <pause 0.3s> Npm test prints three thousand lines of output \\u2014 and every one of them comes straight back into context. Two tests still fail. <pause 0.3s> So it re-reads all three files, plus that three-thousand-line log, just to find what it missed. And re-reading means re-reading everything before it, all over again. One message from you just became a dozen round trips for Claude. <pause 0.3s> Read, write, run, re-read, repeat. You watched one progress bar. Behind it, the conversation quietly doubled in size. This is why a single request can burn ten to a hundred times the tokens of a simple question. <pause 0.6s> You didn't send fifty messages. The loop did. <pause 0.4s> And the longer that loop runs, the more there is to re-read \\u2014 which is exactly where the next trap hides.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nreturn React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,display:'flex',alignItems:'center',justifyContent:'center',fontFamily:D.font_display}},React.createElement(Kit.DotGrid,{}),React.createElement(Kit.Battery,{rem:0.6}),React.createElement('div',{style:{fontSize:R(w*0.02),color:D.text_dim,fontFamily:mono}},'—'));",
    "audio_anchor": "nothing like one",
    "anchor_mode": "appear",
    "source_headline": "One turn to you."
  },
  {
    "framesFrom": 673,
    "framesTo": 922,
    "code": "const NARRATION_TEXT = \"Back to your four-word message. <pause 0.3s> \\\"Fix the login bug.\\\" To you, that's one turn. To Claude Code, it's nothing like one turn. Because Claude Code doesn't just answer. <pause 0.3s> It acts. To fix a login bug, it opens login dot t-s. Then it follows the trail into auth dot t-s, and middleware dot t-s. Three whole files, pulled into context. Then it writes a fix and runs the tests. <pause 0.3s> Npm test prints three thousand lines of output \\u2014 and every one of them comes straight back into context. Two tests still fail. <pause 0.3s> So it re-reads all three files, plus that three-thousand-line log, just to find what it missed. And re-reading means re-reading everything before it, all over again. One message from you just became a dozen round trips for Claude. <pause 0.3s> Read, write, run, re-read, repeat. You watched one progress bar. Behind it, the conversation quietly doubled in size. This is why a single request can burn ten to a hundred times the tokens of a simple question. <pause 0.6s> You didn't send fifty messages. The loop did. <pause 0.4s> And the longer that loop runs, the more there is to re-read \\u2014 which is exactly where the next trap hides.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nreturn React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,display:'flex',alignItems:'center',justifyContent:'center',fontFamily:D.font_display}},React.createElement(Kit.DotGrid,{}),React.createElement(Kit.Battery,{rem:0.6}),React.createElement('div',{style:{fontSize:R(w*0.02),color:D.text_dim,fontFamily:mono}},'—'));",
    "audio_anchor": "three whole files",
    "anchor_mode": "land",
    "source_headline": "Three files, not one."
  },
  {
    "framesFrom": 922,
    "framesTo": 1138,
    "code": "const NARRATION_TEXT = \"Back to your four-word message. <pause 0.3s> \\\"Fix the login bug.\\\" To you, that's one turn. To Claude Code, it's nothing like one turn. Because Claude Code doesn't just answer. <pause 0.3s> It acts. To fix a login bug, it opens login dot t-s. Then it follows the trail into auth dot t-s, and middleware dot t-s. Three whole files, pulled into context. Then it writes a fix and runs the tests. <pause 0.3s> Npm test prints three thousand lines of output \\u2014 and every one of them comes straight back into context. Two tests still fail. <pause 0.3s> So it re-reads all three files, plus that three-thousand-line log, just to find what it missed. And re-reading means re-reading everything before it, all over again. One message from you just became a dozen round trips for Claude. <pause 0.3s> Read, write, run, re-read, repeat. You watched one progress bar. Behind it, the conversation quietly doubled in size. This is why a single request can burn ten to a hundred times the tokens of a simple question. <pause 0.6s> You didn't send fifty messages. The loop did. <pause 0.4s> And the longer that loop runs, the more there is to re-read \\u2014 which is exactly where the next trap hides.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nreturn React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,display:'flex',alignItems:'center',justifyContent:'center',fontFamily:D.font_display}},React.createElement(Kit.DotGrid,{}),React.createElement(Kit.Battery,{rem:0.6}),React.createElement('div',{style:{fontSize:R(w*0.02),color:D.text_dim,fontFamily:mono}},'—'));",
    "audio_anchor": "lines of output",
    "anchor_mode": "land",
    "source_headline": "Three thousand lines."
  },
  {
    "framesFrom": 1138,
    "framesTo": 1541,
    "code": "const NARRATION_TEXT = \"Back to your four-word message. <pause 0.3s> \\\"Fix the login bug.\\\" To you, that's one turn. To Claude Code, it's nothing like one turn. Because Claude Code doesn't just answer. <pause 0.3s> It acts. To fix a login bug, it opens login dot t-s. Then it follows the trail into auth dot t-s, and middleware dot t-s. Three whole files, pulled into context. Then it writes a fix and runs the tests. <pause 0.3s> Npm test prints three thousand lines of output \\u2014 and every one of them comes straight back into context. Two tests still fail. <pause 0.3s> So it re-reads all three files, plus that three-thousand-line log, just to find what it missed. And re-reading means re-reading everything before it, all over again. One message from you just became a dozen round trips for Claude. <pause 0.3s> Read, write, run, re-read, repeat. You watched one progress bar. Behind it, the conversation quietly doubled in size. This is why a single request can burn ten to a hundred times the tokens of a simple question. <pause 0.6s> You didn't send fifty messages. The loop did. <pause 0.4s> And the longer that loop runs, the more there is to re-read \\u2014 which is exactly where the next trap hides.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nreturn React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,display:'flex',alignItems:'center',justifyContent:'center',fontFamily:D.font_display}},React.createElement(Kit.DotGrid,{}),React.createElement(Kit.Battery,{rem:0.6}),React.createElement('div',{style:{fontSize:R(w*0.02),color:D.text_dim,fontFamily:mono}},'—'));",
    "audio_anchor": "re-reads all three",
    "anchor_mode": "through",
    "source_headline": "The loop closes."
  },
  {
    "framesFrom": 1541,
    "framesTo": 2022,
    "code": "const NARRATION_TEXT = \"Back to your four-word message. <pause 0.3s> \\\"Fix the login bug.\\\" To you, that's one turn. To Claude Code, it's nothing like one turn. Because Claude Code doesn't just answer. <pause 0.3s> It acts. To fix a login bug, it opens login dot t-s. Then it follows the trail into auth dot t-s, and middleware dot t-s. Three whole files, pulled into context. Then it writes a fix and runs the tests. <pause 0.3s> Npm test prints three thousand lines of output \\u2014 and every one of them comes straight back into context. Two tests still fail. <pause 0.3s> So it re-reads all three files, plus that three-thousand-line log, just to find what it missed. And re-reading means re-reading everything before it, all over again. One message from you just became a dozen round trips for Claude. <pause 0.3s> Read, write, run, re-read, repeat. You watched one progress bar. Behind it, the conversation quietly doubled in size. This is why a single request can burn ten to a hundred times the tokens of a simple question. <pause 0.6s> You didn't send fifty messages. The loop did. <pause 0.4s> And the longer that loop runs, the more there is to re-read \\u2014 which is exactly where the next trap hides.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nreturn React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,display:'flex',alignItems:'center',justifyContent:'center',fontFamily:D.font_display}},React.createElement(Kit.DotGrid,{}),React.createElement(Kit.Battery,{rem:0.6}),React.createElement('div',{style:{fontSize:R(w*0.02),color:D.text_dim,fontFamily:mono}},'—'));",
    "audio_anchor": "dozen round trips",
    "anchor_mode": "land",
    "source_headline": "A dozen round trips."
  },
  {
    "framesFrom": 2022,
    "framesTo": 2414,
    "code": "const NARRATION_TEXT = \"Back to your four-word message. <pause 0.3s> \\\"Fix the login bug.\\\" To you, that's one turn. To Claude Code, it's nothing like one turn. Because Claude Code doesn't just answer. <pause 0.3s> It acts. To fix a login bug, it opens login dot t-s. Then it follows the trail into auth dot t-s, and middleware dot t-s. Three whole files, pulled into context. Then it writes a fix and runs the tests. <pause 0.3s> Npm test prints three thousand lines of output \\u2014 and every one of them comes straight back into context. Two tests still fail. <pause 0.3s> So it re-reads all three files, plus that three-thousand-line log, just to find what it missed. And re-reading means re-reading everything before it, all over again. One message from you just became a dozen round trips for Claude. <pause 0.3s> Read, write, run, re-read, repeat. You watched one progress bar. Behind it, the conversation quietly doubled in size. This is why a single request can burn ten to a hundred times the tokens of a simple question. <pause 0.6s> You didn't send fifty messages. The loop did. <pause 0.4s> And the longer that loop runs, the more there is to re-read \\u2014 which is exactly where the next trap hides.\";\nconst w=width,h=height;const R=Math.round;const mono=D.font_mono;const CLAMP={extrapolateLeft:'clamp',extrapolateRight:'clamp'};const dur=durationInFrames;\nreturn React.createElement(AbsoluteFill,{style:{backgroundColor:D.bg,display:'flex',alignItems:'center',justifyContent:'center',fontFamily:D.font_display}},React.createElement(Kit.DotGrid,{}),React.createElement(Kit.Battery,{rem:0.6}),React.createElement('div',{style:{fontSize:R(w*0.02),color:D.text_dim,fontFamily:mono}},'—'));",
    "audio_anchor": "of a simple",
    "anchor_mode": "land",
    "source_headline": "Ten to a hundred times."
  }
]