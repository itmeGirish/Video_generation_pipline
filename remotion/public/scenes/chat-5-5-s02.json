[
  {
    "framesFrom": 0,
    "framesTo": 296,
    "code": "const e = React.createElement;\nconst clamp = {extrapolateLeft:'clamp',extrapolateRight:'clamp'};\nconst halfDur = Math.max(1, Math.floor(durationInFrames * 0.5));\n\nconst gridAlpha = interpolate(frame, [0, halfDur], [0, 0.15], clamp);\n\nconst sfX = ((-122.4 + 180) / 360) * width;\nconst sfY = ((90 - 37.7) / 180) * height;\n\nconst dotDelay = Math.max(0, Math.floor(durationInFrames * 0.3));\nconst dotProgress = spring({frame: frame - dotDelay, fps, config: {damping: 14, stiffness: 180}});\n\nconst pulse = interpolate(frame, [dotDelay, Math.max(dotDelay + 1, durationInFrames)], [0, Math.PI * 3], clamp);\nconst glowR = Math.sin(pulse) * 6 + 22;\n\nconst lats = [-60, -30, 0, 30, 60];\nconst latLines = lats.map(function(lat, i) {\n  const bright = lat === 0 ? 0.25 : 1;\n  return e('div', {key:'lt'+i, style:{position:'absolute', left:0, top:((90 - lat) / 180) * height, width:'100%', height: lat === 0 ? 1.5 : 1, backgroundColor: D.text_dim, opacity: gridAlpha * bright}});\n});\n\nconst lons = Array.from({length:13}, function(_, i){ return -180 + i * 30; });\nconst lonLines = lons.map(function(lon, i) {\n  const bright = lon === 0 ? 0.25 : 1;\n  return e('div', {key:'ln'+i, style:{position:'absolute', left:((lon + 180) / 360) * width, top:0, width: lon === 0 ? 1.5 : 1, height:'100%', backgroundColor: D.text_dim, opacity: gridAlpha * bright}});\n});\n\nconst dotSize = 12;\nconst offset = 16;\n\nconst cyanDot = e('div', {key:'cd', style:{\n  position:'absolute',\n  left: sfX - offset - dotSize / 2,\n  top: sfY - dotSize / 2,\n  width: dotSize, height: dotSize, borderRadius:'50%',\n  backgroundColor: D.cyan,\n  boxShadow: '0 0 ' + glowR + 'px ' + D.cyan + ', 0 0 ' + (glowR * 2.5) + 'px ' + D.cyan,\n  transform: 'scale(' + dotProgress + ')',\n  opacity: dotProgress\n}});\n\nconst violetDot = e('div', {key:'vd', style:{\n  position:'absolute',\n  left: sfX + offset - dotSize / 2,\n  top: sfY - dotSize / 2,\n  width: dotSize, height: dotSize, borderRadius:'50%',\n  backgroundColor: D.violet,\n  boxShadow: '0 0 ' + glowR + 'px ' + D.violet + ', 0 0 ' + (glowR * 2.5) + 'px ' + D.violet,\n  transform: 'scale(' + dotProgress + ')',\n  opacity: dotProgress\n}});\n\nconst labelBase = {position:'absolute', fontFamily: D.font_mono, fontSize: 10, letterSpacing: 1.5, textTransform:'uppercase'};\n\nconst cyanLabel = e('div', {key:'cl', style: Object.assign({}, labelBase, {\n  left: sfX - offset - 38,\n  top: sfY - 22,\n  color: D.cyan,\n  opacity: dotProgress * 0.85\n})}, 'Anthropic');\n\nconst violetLabel = e('div', {key:'vl', style: Object.assign({}, labelBase, {\n  left: sfX + offset - 8,\n  top: sfY + 18,\n  color: D.violet,\n  opacity: dotProgress * 0.85\n})}, 'OpenAI');\n\nconst sfLabel = e('div', {key:'sf', style:{\n  position:'absolute',\n  left: sfX - 42,\n  top: sfY + 34,\n  fontFamily: D.font_mono,\n  fontSize: 9,\n  color: D.text_dim,\n  opacity: dotProgress * 0.5,\n  letterSpacing: 1\n}}, 'SAN FRANCISCO');\n\nreturn e('div', {style:{position:'absolute', inset:0, backgroundColor: D.bg, overflow:'hidden'}},\n  e(React.Fragment, null, latLines),\n  e(React.Fragment, null, lonLines),\n  cyanDot, violetDot, cyanLabel, violetLabel, sfLabel\n);",
    "audio_anchor": "April twenty-twenty-six",
    "source_headline": "Black screen.."
  },
  {
    "framesFrom": 296,
    "framesTo": 458,
    "code": "const e = React.createElement;\nconst ox = width * 0.2;\nconst oy = height * 0.72;\nconst launchStart = 3;\nconst launchEnd = Math.max(launchStart + 1, Math.floor(durationInFrames * 0.72));\nconst t = interpolate(frame, [launchStart, launchEnd], [0, 1], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\nconst cpx = ox + width * 0.06;\nconst cpy = height * 0.04;\nconst endx = width * 0.82;\nconst endy = height * 0.14;\nconst bx = (s) => { const a = 1 - s; return a*a*ox + 2*a*s*cpx + s*s*endx; };\nconst by = (s) => { const a = 1 - s; return a*a*oy + 2*a*s*cpy + s*s*endy; };\nconst projX = bx(t);\nconst projY = by(t);\nconst dotScale = spring({frame, fps, config:{damping:D.spring_damping, stiffness:D.spring_stiffness}});\nconst cps = D.type_speed_cps;\nconst elapsed = Math.max(0, (frame - 2) / fps);\nconst l1 = 'APRIL 16';\nconst l2 = 'CLAUDE OPUS 4.7';\nconst c1 = Math.min(l1.length, Math.floor(elapsed * cps));\nconst c2 = Math.min(l2.length, Math.max(0, Math.floor((elapsed - l1.length / cps - 0.08) * cps)));\nconst vp = [];\nfor (let i = 0; i < 12; i++) {\n  const pt = t * ((i + 0.5) / 12);\n  if (pt < 0.001) continue;\n  const age = t - pt;\n  const op = interpolate(age, [0, Math.max(0.01, 0.7)], [0.5, 0.18], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\n  const sz = interpolate(age, [0, Math.max(0.01, 0.7)], [4, 18], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\n  vp.push(e('div', {key:'v'+i, style:{position:'absolute', left:bx(pt)-sz/2, top:by(pt)-sz/2, width:sz, height:sz, borderRadius:'50%', backgroundColor:D.cyan, opacity:op, filter:'blur('+Math.round(sz*0.3)+'px)'}}));\n}\nconst trailPts = [];\nfor (let i = 0; i <= 40; i++) {\n  const s = t * (i / 40);\n  trailPts.push((i === 0 ? 'M' : 'L') + bx(s).toFixed(1) + ',' + by(s).toFixed(1));\n}\nconst glowOp = interpolate(t, [0, Math.max(0.01, 0.05)], [0, 1], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\nconst ch = [];\nif (t > 0.01) {\n  ch.push(e('svg', {key:'trail', style:{position:'absolute', inset:0, width:'100%', height:'100%'}, viewBox:'0 0 ' + width + ' ' + height},\n    e('defs', null,\n      e('filter', {id:'glow'},\n        e('feGaussianBlur', {stdDeviation:'4', result:'b'}),\n        e('feMerge', null, e('feMergeNode', {in:'b'}), e('feMergeNode', {in:'SourceGraphic'}))\n      )\n    ),\n    e('path', {d:trailPts.join(' '), fill:'none', stroke:D.cyan, strokeWidth:3.5, strokeLinecap:'round', opacity:0.5, filter:'url(#glow)'}),\n    e('path', {d:trailPts.join(' '), fill:'none', stroke:D.white, strokeWidth:1.2, strokeLinecap:'round', opacity:0.3})\n  ));\n}\nch.push(...vp);\nch.push(e('div', {key:'dot', style:{position:'absolute', left:ox-12, top:oy-12, width:24, height:24, borderRadius:'50%', backgroundColor:D.cyan, transform:'scale('+dotScale+')', boxShadow:'0 0 20px '+D.cyan+', 0 0 40px '+D.cyan+'80'}}));\nif (t > 0.01) {\n  ch.push(e('div', {key:'proj', style:{position:'absolute', left:projX-10, top:projY-10, width:20, height:20, borderRadius:'50%', backgroundColor:D.cyan, opacity:glowOp, boxShadow:'0 0 30px '+D.cyan+', 0 0 60px '+D.cyan+', 0 0 10px '+D.white}}));\n}\nch.push(e('div', {key:'l1', style:{position:'absolute', left:ox+22, top:oy-12, fontFamily:D.font_mono, fontSize:20, color:D.cyan, letterSpacing:2, whiteSpace:'nowrap', textShadow:'0 0 8px '+D.cyan}}, c1 > 0 ? l1.slice(0, c1) : ''));\nch.push(e('div', {key:'l2', style:{position:'absolute', left:ox+22, top:oy+14, fontFamily:D.font_display, fontSize:26, fontWeight:700, color:D.white, letterSpacing:1, whiteSpace:'nowrap', textShadow:'0 0 10px '+D.cyan+'50'}}, c2 > 0 ? l2.slice(0, c2) : ''));\nreturn e(AbsoluteFill, {style:{backgroundColor:D.bg}}, ...ch);",
    "audio_anchor": "Anthropic drops Claude",
    "source_headline": "From the CYAN dot, a projectile launches UPWARD — a glowing CYAN streak following a ballistic arc (NOT straight).."
  },
  {
    "framesFrom": 458,
    "framesTo": 518,
    "code": "const e = React.createElement;\nconst dur = durationInFrames;\nconst ox = width * 0.1;\nconst oy = height * 0.55;\nconst ex = width * 0.92;\nconst ey = height * 0.42;\nconst arcH = height * 0.35;\nconst startF = 3;\nconst endF = Math.max(startF + 1, dur - 1);\nconst prog = interpolate(frame, [startF, endF], [0, 1], {extrapolateLeft:'clamp',extrapolateRight:'clamp'});\nconst cx = ox + (ex - ox) * prog;\nconst cy = oy + (ey - oy) * prog - arcH * Math.sin(prog * Math.PI);\nconst rot = interpolate(frame, [startF, endF], [0, 360], {extrapolateLeft:'clamp',extrapolateRight:'clamp'});\nconst ds = spring({frame, fps, config:{damping:14, stiffness:180}});\nconst txtOp = interpolate(frame, [0, Math.max(1, 8)], [0, 1], {extrapolateLeft:'clamp',extrapolateRight:'clamp'});\nconst segs = 60;\nconst pts = [];\nfor (let i = 0; i <= segs; i++) {\n  const t = (i / segs) * prog;\n  const px = ox + (ex - ox) * t;\n  const py = oy + (ey - oy) * t - arcH * Math.sin(t * Math.PI);\n  pts.push(i === 0 ? 'M' + px + ',' + py : 'L' + px + ',' + py);\n}\nconst pathStr = pts.join(' ');\nconst trail = e('svg', {style:{position:'absolute',inset:0}, width, height, viewBox:'0 0 '+width+' '+height},\n  e('defs', null,\n    e('linearGradient', {id:'mg', x1:'0%',y1:'0%',x2:'100%',y2:'0%'},\n      e('stop', {offset:'0%', stopColor:D.violet, stopOpacity:0.05}),\n      e('stop', {offset:'100%', stopColor:D.violet, stopOpacity:1})\n    )\n  ),\n  e('path', {d:pathStr, fill:'none', stroke:D.violet, strokeWidth:14, strokeLinecap:'round', opacity:0.12}),\n  e('path', {d:pathStr, fill:'none', stroke:'url(#mg)', strokeWidth:4, strokeLinecap:'round'})\n);\nconst sparkCount = 12;\nconst sparks = Array.from({length:sparkCount}, function(_,i) {\n  const st = Math.max(0, prog - (i * 0.02));\n  if (st <= 0) return null;\n  const sx = ox + (ex - ox) * st;\n  const sy = oy + (ey - oy) * st - arcH * Math.sin(st * Math.PI);\n  const sparkOp = interpolate(i, [0, Math.max(1,sparkCount)], [0.6, 0.1], {extrapolateLeft:'clamp',extrapolateRight:'clamp'});\n  return e('div', {key:'s'+i, style:{position:'absolute', left:sx-2, top:sy-2, width:4, height:4, borderRadius:'50%', background:D.amber, opacity: sparkOp * prog}});\n});\nconst dot = e('div', {style:{position:'absolute', left:ox-18, top:oy-18, width:36, height:36, borderRadius:'50%', background:D.violet, transform:'scale('+ds+')', boxShadow:'0 0 30px '+D.violet}});\nconst dateLbl = e('div', {style:{position:'absolute', left:ox-35, top:oy+28, fontFamily:D.font_mono, fontSize:20, color:D.violet, fontWeight:'bold', opacity:txtOp, whiteSpace:'nowrap'}}, 'APRIL 23');\nconst nameLbl = e('div', {style:{position:'absolute', left:ox-58, top:oy+56, fontFamily:D.font_mono, fontSize:15, color:D.text_dim, opacity:txtOp, whiteSpace:'nowrap', letterSpacing:1}}, \"GPT-5.5 'SPUD'\");\nconst pSize = 72;\nconst pulseOp = interpolate(Math.sin(frame * 0.3), [-1, 1], [0.4, 0.8], {extrapolateLeft:'clamp',extrapolateRight:'clamp'});\nconst glow = e('div', {style:{position:'absolute', left:cx-pSize*0.7, top:cy-pSize*0.7, width:pSize*1.4, height:pSize*1.4, borderRadius:'50%', background:'radial-gradient(circle, '+D.amber+' 0%, transparent 70%)', opacity: pulseOp * prog}});\nconst potato = e('div', {style:{position:'absolute', left:cx-pSize/2, top:cy-pSize*0.35, width:pSize, height:pSize*0.7, transform:'rotate('+rot+'deg)', opacity: prog > 0 ? 1 : 0}},\n  e('svg', {viewBox:'0 0 100 70', width:pSize, height:pSize*0.7, style:{overflow:'visible'}},\n    e('path', {d:'M18,35 C16,14 34,4 56,7 C78,10 92,20 93,37 C94,54 78,65 56,67 C34,69 20,56 18,35Z', fill:D.amber}),\n    e('ellipse', {cx:38,cy:27,rx:5,ry:3.5,fill:D.surface,opacity:0.45}),\n    e('ellipse', {cx:60,cy:22,rx:4,ry:3,fill:D.surface,opacity:0.4}),\n    e('ellipse', {cx:72,cy:40,rx:4,ry:2.5,fill:D.surface,opacity:0.35}),\n    e('ellipse', {cx:42,cy:50,rx:3.5,ry:2,fill:D.surface,opacity:0.3}),\n    e('ellipse', {cx:55,cy:55,rx:3,ry:2,fill:D.surface,opacity:0.25})\n  )\n);\nreturn e('div', {style:{position:'absolute', inset:0, background:D.bg}},\n  trail, ...sparks.filter(Boolean), dot, dateLbl, nameLbl, glow, potato\n);",
    "audio_anchor": "OpenAI fires back",
    "source_headline": "From the MAGENTA dot, a second projectile launches — MAGENTA streak, parallel arc, slightly delayed.."
  },
  {
    "framesFrom": 518,
    "framesTo": 1059,
    "code": "const cx = width / 2;\nconst cy = height / 2;\nconst srand = (seed) => ((seed * 16807) % 2147483647) / 2147483647;\n\nconst flashOpacity = frame <= 6\n  ? interpolate(frame, [0, 6], [0, 0.8], {extrapolateLeft:'clamp', extrapolateRight:'clamp'})\n  : interpolate(frame, [6, 12], [0.8, 0], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\n\nconst ringFrame = Math.max(0, frame - 4);\nconst ringDur = 50;\nconst ringRadius = interpolate(ringFrame, [0, Math.max(1, ringDur)], [0, Math.min(width, height) * 0.55], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\nconst ringOpacity = interpolate(ringFrame, [0, Math.max(1, ringDur)], [0.9, 0], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\nconst ringThickness = interpolate(ringFrame, [0, Math.max(1, ringDur)], [30, 3], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\nconst halfCirc = Math.PI * Math.max(1, ringRadius);\n\nconst particleCount = 120;\nconst particleEls = Array.from({length: particleCount}, function(_, i) {\n  const s1 = srand(i * 7 + 13);\n  const s2 = srand(i * 11 + 29);\n  const s3 = srand(i * 17 + 41);\n  const s4 = srand(i * 23 + 53);\n  const angle = s1 * Math.PI * 2;\n  const speed = 3 + s2 * 10;\n  const spinRate = (s3 - 0.5) * 600;\n  const size = 2 + s4 * 6;\n  const pFrame = Math.max(0, frame - 6);\n  const dist = speed * pFrame;\n  const x = cx + Math.cos(angle) * dist;\n  const y = cy + Math.sin(angle) * dist;\n  const opacity = interpolate(pFrame, [0, 40], [1, 0], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\n  const rotation = spinRate * (pFrame / fps);\n  const color = i % 2 === 0 ? D.cyan : D.violet;\n  return React.createElement('div', {\n    key: 'p' + i,\n    style: {\n      position: 'absolute',\n      left: x - size / 2,\n      top: y - size / 2,\n      width: size,\n      height: size,\n      borderRadius: s4 > 0.5 ? '50%' : '2px',\n      backgroundColor: color,\n      opacity: opacity,\n      transform: 'rotate(' + rotation + 'deg)',\n      boxShadow: '0 0 6px ' + color\n    }\n  });\n});\n\nconst vsDelay = 14;\nconst vsOpacity = interpolate(frame, [vsDelay, Math.max(vsDelay + 1, vsDelay + 6)], [0, 1], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\nconst vsScale = frame >= vsDelay ? spring({frame: frame - vsDelay, fps, config: {damping: 12, stiffness: 200}}) : 0;\nconst glowSize = frame >= vsDelay ? interpolate((frame - vsDelay) % 40, [0, 20, 40], [10, 28, 10], {extrapolateLeft:'clamp', extrapolateRight:'clamp'}) : 0;\n\nconst flashEl = React.createElement('div', {key: 'flash', style: {\n  position: 'absolute', inset: 0,\n  backgroundColor: D.white,\n  opacity: flashOpacity,\n  pointerEvents: 'none'\n}});\n\nconst ringEl = React.createElement('svg', {\n  key: 'ring',\n  style: {position: 'absolute', left: 0, top: 0, width: '100%', height: '100%', opacity: ringOpacity},\n  viewBox: '0 0 ' + width + ' ' + height\n},\n  React.createElement('circle', {\n    cx: cx, cy: cy, r: ringRadius,\n    fill: 'none',\n    stroke: D.cyan,\n    strokeWidth: ringThickness,\n    strokeDasharray: halfCirc + ' ' + halfCirc,\n    strokeDashoffset: 0\n  }),\n  React.createElement('circle', {\n    cx: cx, cy: cy, r: ringRadius,\n    fill: 'none',\n    stroke: D.violet,\n    strokeWidth: ringThickness,\n    strokeDasharray: halfCirc + ' ' + halfCirc,\n    strokeDashoffset: -halfCirc\n  })\n);\n\nconst particlesEl = React.createElement('div', {key: 'parts', style: {position: 'absolute', inset: 0, pointerEvents: 'none'}}, ...particleEls);\n\nconst vsEl = React.createElement('div', {key: 'vs', style: {\n  position: 'absolute',\n  left: cx,\n  top: cy,\n  transform: 'translate(-50%,-50%) scale(' + vsScale + ')',\n  opacity: vsOpacity,\n  fontFamily: D.font_display,\n  fontSize: 180,\n  fontWeight: 900,\n  color: D.white,\n  textShadow: '0 0 ' + glowSize + 'px ' + D.white + ', 0 0 ' + (glowSize * 2) + 'px rgba(255,255,255,0.4)',\n  letterSpacing: -4\n}}, 'VS');\n\nreturn React.createElement('div', {style: {position: 'absolute', inset: 0, backgroundColor: D.bg, overflow: 'hidden'}}, flashEl, ringEl, particlesEl, vsEl);",
    "audio_anchor": "Seven days apart",
    "source_headline": "Both arcs converge at center screen.."
  },
  {
    "framesFrom": 1059,
    "framesTo": 1119,
    "code": "const e = React.createElement;\nconst dur = durationInFrames;\n\nconst numParticles = 40;\nconst particles = Array.from({length: numParticles}, function(_, i) {\n  const seed = i * 137.508;\n  const angle = (seed % 360) * Math.PI / 180;\n  const baseRadius = 180 + (i % 5) * 30;\n  const settleProgress = interpolate(frame, [0, Math.max(1, Math.floor(dur * 0.6))], [0, 1], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\n  const radius = baseRadius * (1 - settleProgress * 0.8);\n  const opacity = interpolate(frame, [0, Math.max(1, Math.floor(dur * 0.5))], [0.5, 0], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\n  const x = width * 0.5 + Math.cos(angle + frame * 0.004) * radius;\n  const y = height * 0.5 + Math.sin(angle + frame * 0.004) * radius * 0.5;\n  var sz = 2 + (i % 3);\n  var clr = i % 3 === 0 ? D.cyan : i % 3 === 1 ? D.violet : D.amber;\n  return e('div', {key:'p'+i, style:{position:'absolute', left:x, top:y, width:sz, height:sz, borderRadius:'50%', background:clr, opacity:opacity, filter:'blur(1px)'}});\n});\n\nvar mapOpacity = interpolate(frame, [Math.floor(dur*0.15), Math.max(Math.floor(dur*0.15)+1, Math.floor(dur*0.55))], [0, 0.15], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\nvar gridDots = [];\nfor (var row = 0; row < 20; row++) {\n  for (var col = 0; col < 35; col++) {\n    var nx = col / 35;\n    var ny = row / 20;\n    var land = (nx>0.12&&nx<0.32&&ny>0.15&&ny<0.55)||(nx>0.38&&nx<0.53&&ny>0.08&&ny<0.42)||(nx>0.44&&nx<0.62&&ny>0.28&&ny<0.65)||(nx>0.58&&nx<0.82&&ny>0.12&&ny<0.48)||(nx>0.73&&nx<0.88&&ny>0.55&&ny<0.72);\n    if (!land) continue;\n    gridDots.push(e('div', {key:'m'+row+'_'+col, style:{position:'absolute', left:width*0.08+col*(width*0.84/35), top:height*0.12+row*(height*0.65/20), width:3, height:3, borderRadius:'50%', background:D.cyan, opacity:mapOpacity}}));\n  }\n}\n\nvar nebulaRingOpacity = interpolate(frame, [0, Math.max(1, Math.floor(dur*0.5))], [0.35, 0], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\nvar ringScale = interpolate(frame, [0, Math.max(1, Math.floor(dur*0.5))], [1, 0.6], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\nvar nebulaRing = e('div', {style:{position:'absolute', left:width*0.5, top:height*0.42, width:420, height:180, marginLeft:-210, marginTop:-90, borderRadius:'50%', border:'2px solid '+D.violet, boxShadow:'0 0 40px '+D.violet+', inset 0 0 30px '+D.cyan, opacity:nebulaRingOpacity, transform:'scale('+ringScale+')'}});\n\nvar vsOpacity = interpolate(frame, [0, Math.max(1, Math.floor(dur*0.3))], [0.7, 0.35], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\nvar vsText = e('div', {style:{position:'absolute', top:height*0.36, width:'100%', textAlign:'center', fontFamily:D.font_display, fontSize:68, fontWeight:900, color:D.white, opacity:vsOpacity, letterSpacing:12}}, 'VS');\n\nvar typingStr = '7 DAYS APART';\nvar typeStart = Math.floor(dur * 0.2);\nvar cps = D.type_speed_cps || 12;\nvar fpc = Math.max(1, Math.round(fps / cps));\nvar charsVis = Math.min(typingStr.length, Math.max(0, Math.floor((frame - typeStart) / fpc)));\nvar shown = typingStr.substring(0, charsVis);\nvar cursorOn = frame > typeStart && (Math.floor(frame / 8) % 2 === 0 || charsVis < typingStr.length);\nvar daysApart = e('div', {style:{position:'absolute', top:height*0.50, width:'100%', textAlign:'center', fontFamily:D.font_mono, fontSize:36, fontWeight:700, color:D.amber, letterSpacing:5, opacity:frame>typeStart?1:0}}, shown + (cursorOn ? '|' : ''));\n\nvar underline = e('div', {style:{position:'absolute', top:height*0.50+48, left:'50%', marginLeft:-100, width:200, height:2, background:D.amber, opacity:interpolate(frame, [typeStart + typingStr.length*fpc, Math.max(typeStart+typingStr.length*fpc+1, typeStart+typingStr.length*fpc+15)], [0, 0.7], {extrapolateLeft:'clamp', extrapolateRight:'clamp'})}});\n\nvar logoDelay = Math.floor(dur * 0.55);\nvar logoOp = interpolate(frame, [logoDelay, Math.max(logoDelay+1, logoDelay+Math.floor(dur*0.3))], [0, 0.9], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\nvar logoY = interpolate(frame, [logoDelay, Math.max(logoDelay+1, logoDelay+Math.floor(dur*0.3))], [12, 0], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\nvar logo = e('div', {style:{position:'absolute', bottom:height*0.06, left:'50%', transform:'translateX(-50%) translateY('+logoY+'px)', display:'flex', alignItems:'center', gap:10, opacity:logoOp}},\n  e('div', {style:{width:0, height:0, borderLeft:'14px solid '+D.cyan, borderTop:'9px solid transparent', borderBottom:'9px solid transparent'}}),\n  e('div', {style:{fontFamily:D.font_display, fontSize:16, color:D.text_dim, letterSpacing:3, textTransform:'uppercase'}}, 'EXPLAIN AI')\n);\n\nvar bg = e('div', {style:{position:'absolute', inset:0, background:D.bg}});\n\nvar args = [React.Fragment, null, bg].concat(gridDots).concat(particles).concat([nebulaRing, vsText, daysApart, underline, logo]);\nreturn e.apply(null, args);",
    "audio_anchor": "arms race on Earth",
    "source_headline": "Particles settle.."
  }
]