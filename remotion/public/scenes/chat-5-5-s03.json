[
  {
    "framesFrom": 0,
    "framesTo": 101,
    "code": "const e = React.createElement;\nconst clamp = { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' };\n\nconst edFade = interpolate(frame, [0, Math.max(1, 12)], [0, 1], clamp);\n\nconst drop = spring({ frame, fps, config: { damping: D.spring_damping, stiffness: D.spring_stiffness } });\nconst cubeY = interpolate(drop, [0, 1], [-200, height * 0.35]);\nconst glow = interpolate(frame, [0, Math.max(1, durationInFrames - 1)], [12, 30], clamp);\n\nconst lines = Array.from({ length: 14 }, function(_, i) {\n  var widths = [0.55, 0.4, 0.25, 0.5, 0.15, 0.6, 0.35, 0.45, 0.3, 0.65, 0.2, 0.45, 0.55, 0.25];\n  var colors = [D.cyan, D.violet, D.text_dim, D.cyan, D.text_dim, D.amber, D.text_dim, D.violet, D.cyan, D.text_dim, D.green, D.cyan, D.violet, D.text_dim];\n  return { w: widths[i], c: colors[i] };\n});\n\nconst pad = width * 0.08;\nconst cubeSize = Math.min(width, height) * 0.12;\n\nreturn e(AbsoluteFill, { style: { background: D.bg } },\n  e('div', { style: {\n    position: 'absolute', left: pad, top: pad,\n    width: width - pad * 2, height: height - pad * 2,\n    background: D.surface, borderRadius: 16,\n    border: '2px solid ' + D.text_dim,\n    overflow: 'hidden', opacity: edFade,\n  } },\n    e('div', { style: { height: 44, background: D.bg, borderBottom: '1px solid ' + D.text_dim, display: 'flex', alignItems: 'center', padding: '0 16px', gap: 8 } },\n      e('div', { key: 'd0', style: { width: 14, height: 14, borderRadius: '50%', background: D.red } }),\n      e('div', { key: 'd1', style: { width: 14, height: 14, borderRadius: '50%', background: D.amber } }),\n      e('div', { key: 'd2', style: { width: 14, height: 14, borderRadius: '50%', background: D.green } }),\n      e('span', { style: { marginLeft: 16, color: D.text_dim, fontFamily: D.font_mono, fontSize: 14 } }, 'main.py')\n    ),\n    e('div', { style: { padding: '16px 0' } },\n      lines.map(function(ln, i) {\n        return e('div', { key: 'ln' + i, style: { display: 'flex', alignItems: 'center', height: 28, padding: '0 20px' } },\n          e('span', { style: { width: 36, color: D.text_dim, fontFamily: D.font_mono, fontSize: 13, textAlign: 'right', marginRight: 16, opacity: 0.4 } }, String(i + 1)),\n          e('div', { style: { height: 10, width: (ln.w * 100) + '%', background: ln.c, borderRadius: 4, opacity: 0.5 } })\n        );\n      })\n    )\n  ),\n  e('div', { style: {\n    position: 'absolute',\n    left: width / 2 - cubeSize / 2,\n    top: cubeY,\n    width: cubeSize, height: cubeSize,\n    background: D.white,\n    borderRadius: 10,\n    transform: 'perspective(800px) rotateX(-10deg) rotateY(12deg)',\n    boxShadow: '0 0 ' + glow + 'px ' + (glow * 0.7) + 'px ' + D.white + ', 0 0 ' + (glow * 2.5) + 'px ' + glow + 'px ' + D.cyan,\n    display: 'flex', alignItems: 'center', justifyContent: 'center',\n    opacity: drop,\n  } },\n    e('span', { style: {\n      fontFamily: D.font_display, fontSize: cubeSize * 0.16,\n      fontWeight: 800, color: D.bg, letterSpacing: 2,\n      textTransform: 'uppercase', textAlign: 'center',\n    } }, 'BUG FIX')\n  )\n);",
    "audio_anchor": "Before I throw benchmarks",
    "source_headline": "A flat 2D workspace appears — abstracted code editor.."
  },
  {
    "framesFrom": 101,
    "framesTo": 1120,
    "code": "const e = React.createElement;\nconst F = React.Fragment;\nconst dur = durationInFrames;\nconst si = (a,b,c,d) => interpolate(frame,[a,Math.max(a+1,b)],[c,d],{extrapolateLeft:'clamp',extrapolateRight:'clamp'});\nconst step = Math.max(2, Math.floor(dur/6));\nconst phase = Math.min(5, Math.floor(frame/step));\nconst pf = frame - phase * step;\nconst spE = spring({frame:pf,fps,config:{damping:D.spring_damping,stiffness:D.spring_stiffness}});\nconst cx = width*0.58, cy = height*0.38, benchY = height*0.62;\n\nconst bg = e('div',{style:{position:'absolute',inset:0,background:resolveColor('bg')}});\nconst grid = e('div',{style:{position:'absolute',inset:0,opacity:D.dot_grid_opacity,backgroundImage:'radial-gradient(circle,'+resolveColor('text_dim')+' 1px,transparent 1px)',backgroundSize:D.dot_grid_spacing+'px '+D.dot_grid_spacing+'px'}});\n\nconst wb = e(F,null,\n  e('div',{style:{position:'absolute',left:width*0.25,top:benchY,width:width*0.55,height:8,background:resolveColor('text_dim'),borderRadius:4}}),\n  e('div',{style:{position:'absolute',left:width*0.28,top:benchY+8,width:5,height:height*0.09,background:resolveColor('text_dim')}}),\n  e('div',{style:{position:'absolute',left:width*0.76,top:benchY+8,width:5,height:height*0.09,background:resolveColor('text_dim')}})\n);\n\nconst fX = width*0.2, fTop = benchY-168;\nconst armA = Math.sin(frame*0.25)*8+20;\nconst fig = e(F,null,\n  e('div',{style:{position:'absolute',left:fX-16,top:fTop,width:32,height:32,borderRadius:'50%',background:resolveColor('cyan')}}),\n  e('div',{style:{position:'absolute',left:fX+2,top:fTop+11,width:8,height:8,borderRadius:'50%',background:resolveColor('bg')}}),\n  e('div',{style:{position:'absolute',left:fX-3,top:fTop+32,width:6,height:10,background:resolveColor('cyan')}}),\n  e('div',{style:{position:'absolute',left:fX-18,top:fTop+42,width:36,height:50,background:resolveColor('cyan'),borderRadius:3}}),\n  e('div',{style:{position:'absolute',left:fX-36,top:fTop+46,width:20,height:6,background:resolveColor('cyan'),borderRadius:3,transformOrigin:'right center',transform:'rotate(-15deg)'}}),\n  e('div',{style:{position:'absolute',left:fX+18,top:fTop+46,width:20,height:6,background:resolveColor('cyan'),borderRadius:3,transformOrigin:'left center',transform:'rotate('+armA+'deg)'}}),\n  e('div',{style:{position:'absolute',left:fX-12,top:fTop+92,width:8,height:40,background:resolveColor('cyan'),borderRadius:3}}),\n  e('div',{style:{position:'absolute',left:fX+4,top:fTop+92,width:8,height:40,background:resolveColor('cyan'),borderRadius:3}})\n);\n\nconst phaseLabels = ['EXAMINE','UNFOLD','WRITE FIX','BUILD TEST','RUN TEST','SEAL'];\nconst label = e('div',{style:{position:'absolute',left:cx-60,top:cy-80,fontFamily:D.font_mono,fontSize:13,color:resolveColor('cyan'),opacity:0.8,letterSpacing:2,textAlign:'center',width:120,transform:'scale('+spE+')'}},phaseLabels[phase]);\n\nconst stepDots = e('div',{style:{position:'absolute',bottom:height*0.1,left:0,right:0,display:'flex',justifyContent:'center',gap:12}},\n  Array.from({length:6},function(_,i){return e('div',{key:'d'+i,style:{width:8,height:8,borderRadius:'50%',background:i<=phase?resolveColor('cyan'):resolveColor('text_dim'),opacity:i<=phase?1:0.3}})})\n);\n\nconst layers = [];\n\nif(phase===0){\n  var rot = si(0,step,0,360);\n  var mag = spring({frame:Math.max(0,pf-3),fps,config:{damping:14,stiffness:200}});\n  layers.push(e('div',{key:'p0',style:{position:'absolute',left:cx-35,top:cy-35}},\n    e('div',{style:{width:70,height:70,border:'3px solid '+resolveColor('cyan'),borderRadius:6,transform:'rotate('+rot+'deg)',display:'flex',alignItems:'center',justifyContent:'center',fontFamily:D.font_mono,fontSize:14,color:resolveColor('text_dim')}},'</>'),\n    e('div',{style:{position:'absolute',right:-32,top:-12,transform:'scale('+mag+')'}},\n      e('div',{style:{width:24,height:24,borderRadius:'50%',border:'3px solid '+resolveColor('white')}}),\n      e('div',{style:{position:'absolute',bottom:-8,right:-4,width:3,height:14,background:resolveColor('white'),transform:'rotate(-45deg)',borderRadius:2}})\n    )\n  ));\n}\n\nif(phase===1){\n  var uw = si(0,Math.floor(step*0.5),10,160);\n  var uh = si(0,Math.floor(step*0.5),10,100);\n  var clOp = si(Math.floor(step*0.4),step,0,0.7);\n  layers.push(e('div',{key:'p1',style:{position:'absolute',left:cx-80,top:cy-50}},\n    e('div',{style:{width:uw,height:uh,background:resolveColor('surface'),border:'2px solid '+resolveColor('cyan'),borderRadius:4,overflow:'hidden',padding:uw>60?8:0}},\n      uw>80?Array.from({length:5},function(_,i){return e('div',{key:'l'+i,style:{height:3,marginBottom:5,borderRadius:2,background:resolveColor('text_dim'),width:(30+i*14)+'%',opacity:clOp}})}):null\n    )\n  ));\n}\n\nif(phase===2){\n  var dp = si(0,step,0,1);\n  layers.push(e('div',{key:'p2',style:{position:'absolute',left:cx-80,top:cy-50}},\n    e('div',{style:{width:160,height:100,background:resolveColor('surface'),border:'2px solid '+resolveColor('cyan'),borderRadius:4,padding:8}},\n      Array.from({length:3},function(_,i){return e('div',{key:'o'+i,style:{height:3,marginBottom:5,borderRadius:2,background:resolveColor('text_dim'),width:(45+i*10)+'%',opacity:0.35}})}),\n      Array.from({length:3},function(_,i){return e('div',{key:'n'+i,style:{height:3,marginBottom:5,borderRadius:2,background:resolveColor('green'),width:(dp*(35+i*12))+'%',opacity:dp>0.05?1:0}})})\n    ),\n    e('div',{style:{position:'absolute',left:8+dp*130,top:48+dp*25,width:0,height:0,borderLeft:'5px solid transparent',borderRight:'5px solid transparent',borderTop:'14px solid '+resolveColor('green'),transform:'rotate(135deg)'}})\n  ));\n}\n\nif(phase===3){\n  var th = si(0,Math.floor(step*0.7),0,65);\n  var lOp = si(Math.floor(step*0.6),step,0,1);\n  layers.push(e('div',{key:'p3',style:{position:'absolute',left:cx-12,top:cy-35}},\n    e('div',{style:{width:24,height:th,border:'3px solid '+resolveColor('violet'),borderRadius:'0 0 12px 12px'}}),\n    e('div',{style:{fontFamily:D.font_mono,fontSize:10,color:resolveColor('violet'),textAlign:'center',marginTop:4,opacity:lOp,fontWeight:'bold',letterSpacing:1}},'TEST')\n  ));\n}\n\nif(phase===4){\n  var fp = si(0,Math.floor(step*0.55),0,100);\n  var cs = spring({frame:Math.max(0,pf-Math.floor(step*0.5)),fps,config:{damping:10,stiffness:200}});\n  layers.push(e('div',{key:'p4',style:{position:'absolute',left:cx-12,top:cy-35}},\n    e('div',{style:{width:24,height:65,border:'3px solid '+resolveColor('green'),borderRadius:'0 0 12px 12px',position:'relative',overflow:'hidden'}},\n      e('div',{style:{position:'absolute',bottom:0,left:0,right:0,height:fp+'%',background:resolveColor('green'),opacity:0.5,borderRadius:'0 0 9px 9px'}})\n    ),\n    e('div',{style:{position:'absolute',left:32,top:8,fontSize:26,color:resolveColor('green'),transform:'scale('+cs+')',fontWeight:'bold'}},'\\u2713')\n  ));\n}\n\nif(phase===5){\n  var cs2 = spring({frame:pf,fps,config:{damping:D.spring_damping,stiffness:D.spring_stiffness}});\n  var ss = spring({frame:Math.max(0,pf-Math.floor(step*0.4)),fps,config:{damping:10,stiffness:180}});\n  layers.push(e('div',{key:'p5',style:{position:'absolute',left:cx-40,top:cy-40}},\n    e('div',{style:{width:80,height:80,border:'3px solid '+resolveColor('cyan'),borderRadius:8,transform:'scale('+cs2+')',display:'flex',alignItems:'center',justifyContent:'center'}},\n      e('div',{style:{width:60,height:60,background:resolveColor('cyan'),opacity:0.15,borderRadius:4}})\n    ),\n    e('div',{style:{position:'absolute',left:8,top:28,transform:'scale('+ss+') rotate(-12deg)',border:'3px solid '+resolveColor('green'),borderRadius:5,padding:'3px 8px',fontFamily:D.font_mono,fontSize:12,color:resolveColor('green'),fontWeight:'bold',letterSpacing:2,background:resolveColor('bg')}},'VERIFIED')\n  ));\n}\n\nreturn e('div',{style:{position:'absolute',inset:0,overflow:'hidden'}},bg,grid,wb,fig,label,stepDots,layers[0]||null);",
    "audio_anchor": "comparison videos completely skip",
    "source_headline": "A CYAN figure (geometric humanoid — clean rectangles and circles, Bauhaus-style) picks up the cube and places it on a WORKBENCH.."
  },
  {
    "framesFrom": 1120,
    "framesTo": 1180,
    "code": "const e = React.createElement;\nconst dur = durationInFrames;\n\nconst prog = spring({ frame, fps, config: { damping: 14, stiffness: 160 } });\n\nconst figX = width * 0.26;\nconst figY = height * 0.38;\n\nconst cubeX = interpolate(prog, [0, 1], [figX + 90, width * 0.6]);\nconst cubeY = interpolate(prog, [0, 1], [figY - 10, height * 0.24]);\nconst cubeScl = interpolate(prog, [0, 1], [0.65, 1.18]);\nconst cubeRot = interpolate(prog, [0, 1], [-14, 2]);\nconst pulse = 16 + Math.sin(frame * 0.15) * 7;\n\nconst checkDelay = Math.max(0, frame - Math.floor(dur * 0.22));\nconst checkP = spring({ frame: checkDelay, fps, config: { damping: 10, stiffness: 200 } });\n\nconst armReach = interpolate(prog, [0, 1], [20, 75]);\n\nconst t1s = Math.floor(dur * 0.28);\nconst t1e = Math.max(t1s + 1, Math.floor(dur * 0.46));\nconst t1Op = interpolate(frame, [t1s, t1e], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\nconst t1Y = interpolate(frame, [t1s, t1e], [28, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\n\nconst t2s = Math.floor(dur * 0.46);\nconst t2e = Math.max(t2s + 1, Math.floor(dur * 0.62));\nconst t2Op = interpolate(frame, [t2s, t2e], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\nconst t2Y = interpolate(frame, [t2s, t2e], [28, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\n\nreturn e('div', { style: { position: 'absolute', inset: 0, background: D.bg, overflow: 'hidden' } },\n\n  e('div', { style: {\n    position: 'absolute',\n    left: figX - 200, top: figY - 240,\n    width: 460, height: 460,\n    borderRadius: '50%',\n    background: 'radial-gradient(circle, ' + D.cyan + '10 0%, transparent 65%)',\n  }}),\n\n  e('svg', {\n    width: 200, height: 290,\n    viewBox: '0 0 200 290',\n    style: {\n      position: 'absolute',\n      left: figX - 100,\n      top: figY - 50,\n      filter: 'drop-shadow(0 0 18px ' + D.cyan + '55)',\n    }\n  },\n    e('circle', { cx: 100, cy: 40, r: 33, fill: D.cyan, opacity: 0.92 }),\n    e('path', { d: 'M58,86 L142,86 L152,215 L48,215 Z', fill: D.cyan, opacity: 0.72 }),\n    e('line', {\n      x1: 142, y1: 112,\n      x2: 142 + armReach, y2: 112 - armReach * 0.35,\n      stroke: D.cyan, strokeWidth: 10, strokeLinecap: 'round', opacity: 0.88\n    }),\n    e('line', {\n      x1: 58, y1: 112, x2: 26, y2: 160,\n      stroke: D.cyan, strokeWidth: 10, strokeLinecap: 'round', opacity: 0.88\n    }),\n    e('line', { x1: 78, y1: 215, x2: 62, y2: 278, stroke: D.cyan, strokeWidth: 10, strokeLinecap: 'round', opacity: 0.6 }),\n    e('line', { x1: 122, y1: 215, x2: 138, y2: 278, stroke: D.cyan, strokeWidth: 10, strokeLinecap: 'round', opacity: 0.6 })\n  ),\n\n  e('div', { style: {\n    position: 'absolute',\n    left: cubeX - 52, top: cubeY - 52,\n    width: 104, height: 104,\n    transform: 'scale(' + cubeScl + ') rotate(' + cubeRot + 'deg)',\n    transformOrigin: 'center',\n  }},\n    e('div', { style: {\n      width: 104, height: 104,\n      background: 'linear-gradient(135deg, ' + D.cyan + '28, ' + D.cyan + '80)',\n      border: '2.5px solid ' + D.cyan,\n      borderRadius: 10,\n      boxShadow: '0 0 ' + pulse + 'px ' + D.cyan + ', inset 0 0 28px ' + D.cyan + '18',\n      display: 'flex', alignItems: 'center', justifyContent: 'center',\n    }},\n      e('svg', {\n        width: 50, height: 50, viewBox: '0 0 50 50',\n        style: { opacity: checkP, transform: 'scale(' + checkP + ')' }\n      },\n        e('polyline', {\n          points: '12,26 21,36 38,14',\n          stroke: D.green, strokeWidth: 5, fill: 'none',\n          strokeLinecap: 'round', strokeLinejoin: 'round',\n        })\n      )\n    )\n  ),\n\n  Array.from({ length: 6 }, function(_, i) {\n    var delay = i * 3;\n    var pFrame = Math.max(0, frame - delay);\n    var pOp = interpolate(pFrame, [0, Math.max(1, Math.floor(dur * 0.5))], [0.55, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\n    var pX = interpolate(prog, [0, 1], [figX + 55 + i * 10, cubeX - 25 - i * 14]);\n    var pY = cubeY + (i - 2.5) * 14;\n    return e('div', {\n      key: 'p' + i,\n      style: {\n        position: 'absolute',\n        left: pX, top: pY,\n        width: 5, height: 5,\n        borderRadius: '50%',\n        background: D.cyan,\n        opacity: pOp,\n        boxShadow: '0 0 8px ' + D.cyan,\n      }\n    });\n  }),\n\n  e('div', { style: {\n    position: 'absolute',\n    bottom: height * 0.11,\n    left: 0, right: 0,\n    textAlign: 'center',\n  }},\n    e('div', { style: {\n      fontFamily: D.font_display,\n      fontSize: Math.min(58, width * 0.04),\n      fontWeight: 900,\n      color: D.cyan,\n      letterSpacing: 3,\n      textShadow: '0 0 32px ' + D.cyan + '88, 0 0 64px ' + D.cyan + '44',\n      opacity: t1Op,\n      transform: 'translateY(' + t1Y + 'px)',\n    }},\n      '\\u201CHAND ME THE HARD STUFF.\\u201D'\n    ),\n    e('div', { style: {\n      fontFamily: D.font_display,\n      fontSize: Math.min(58, width * 0.04),\n      fontWeight: 900,\n      color: D.cyan,\n      letterSpacing: 3,\n      textShadow: '0 0 32px ' + D.cyan + '88, 0 0 64px ' + D.cyan + '44',\n      opacity: t2Op,\n      transform: 'translateY(' + t2Y + 'px)',\n      marginTop: 10,\n    }},\n      '\\u201CWALK AWAY.\\u201D'\n    )\n  )\n);",
    "audio_anchor": "hand me the hard stuff",
    "source_headline": "The CYAN figure hands the verified cube back.."
  },
  {
    "framesFrom": 1180,
    "framesTo": 1349,
    "code": "const safeEnd = (s, d) => Math.max(s + 1, s + d);\nconst stats = [['SWE-bench', '87.6%'], ['Hallucination', '36%'], ['', '$5 / $25']];\nconst engDur = Math.floor(durationInFrames * 0.22);\nconst stagger = Math.floor(durationInFrames * 0.26);\nconst t0 = Math.floor(durationInFrames * 0.06);\nconst bL = width * 0.08, bT = height * 0.15, bW = width * 0.84, bH = height * 0.7;\nconst rowGap = bH * 0.24, y0 = bT + bH * 0.16;\nconst labelFs = Math.floor(width * 0.026), valFs = Math.floor(width * 0.05);\n\nconst grain = Array.from({ length: 28 }, (_, i) =>\n  React.createElement('div', { key: 'g' + i, style: {\n    position: 'absolute', left: bL, top: bT + (i / 28) * bH,\n    width: bW, height: i % 3 === 0 ? 2 : 1,\n    background: D.text_dim, opacity: 0.03 + (i % 5) * 0.012, borderRadius: 1\n  }})\n);\n\nconst rows = stats.map(function(pair, i) {\n  var label = pair[0], value = pair[1];\n  var s0 = t0 + i * stagger;\n  var s1 = safeEnd(s0, engDur);\n  var p = interpolate(frame, [s0, s1], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\n  if (p <= 0) return null;\n  var y = y0 + i * rowGap;\n  var cutting = p > 0 && p < 1;\n  var hasLabel = label.length > 0;\n\n  var children = [];\n  if (hasLabel) {\n    children.push(React.createElement('span', { key: 'lb', style: {\n      fontFamily: D.font_mono, fontSize: labelFs, color: D.text_dim,\n      letterSpacing: '0.1em', textTransform: 'uppercase',\n      textShadow: '0 1px 0 rgba(255,255,255,0.04), 0 -1px 0 rgba(0,0,0,0.5)'\n    }}, label));\n  }\n  children.push(React.createElement('span', { key: 'vl', style: {\n    fontFamily: D.font_display, fontSize: valFs, fontWeight: 700, color: D.amber,\n    textShadow: '0 2px 0 rgba(0,0,0,0.5), 0 -1px 0 rgba(255,255,255,0.05)'\n  }}, value));\n\n  var textRow = React.createElement('div', { key: 'row' + i, style: {\n    position: 'absolute', left: bL + bW * 0.06, top: y, width: bW * 0.88,\n    display: 'flex', justifyContent: hasLabel ? 'space-between' : 'center',\n    alignItems: 'baseline',\n    clipPath: 'inset(0 ' + (100 - p * 100) + '% 0 0)'\n  }}, ...children);\n\n  var laser = cutting ? React.createElement('div', { key: 'lz' + i, style: {\n    position: 'absolute', left: bL + bW * 0.06 + bW * 0.88 * p - 1.5,\n    top: y - 10, width: 3, height: valFs + 20,\n    background: D.cyan, borderRadius: 2,\n    boxShadow: '0 0 14px 5px ' + D.cyan + ', 0 0 40px 12px ' + D.cyan,\n    opacity: interpolate(p, [0, 0.03, 0.93, 1], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })\n  }}) : null;\n\n  var sparks = cutting ? Array.from({ length: 5 }, function(_, j) {\n    var h = (i * 17 + j * 31 + frame * 7) % 100;\n    return React.createElement('div', { key: 'sp' + i + j, style: {\n      position: 'absolute',\n      left: bL + bW * 0.06 + bW * 0.88 * p + (h % 22 - 11),\n      top: y + (h % 50 - 20), width: 3, height: 3, borderRadius: '50%',\n      background: D.amber, opacity: ((h + frame * 5) % 28) < 10 ? 0.85 : 0,\n      boxShadow: '0 0 6px ' + D.amber\n    }});\n  }) : [];\n\n  return React.createElement(React.Fragment, { key: 'fr' + i }, textRow, laser, ...sparks);\n});\n\nvar boltPos = [[bL + 18, bT + 18], [bL + bW - 28, bT + 18], [bL + 18, bT + bH - 28], [bL + bW - 28, bT + bH - 28]];\nvar bolts = boltPos.map(function(xy, i) {\n  return React.createElement('div', { key: 'bt' + i, style: {\n    position: 'absolute', left: xy[0], top: xy[1], width: 10, height: 10,\n    borderRadius: '50%', background: D.text_dim,\n    boxShadow: 'inset 0 -1px 2px rgba(0,0,0,0.6)', opacity: 0.4\n  }});\n});\n\nvar divs = [0, 1].map(function(i) {\n  var dy = y0 + (i + 1) * rowGap - rowGap * 0.35;\n  var dStart = safeEnd(t0 + i * stagger, engDur);\n  var dEnd = safeEnd(dStart, Math.floor(durationInFrames * 0.06));\n  var dOp = interpolate(frame, [dStart, dEnd], [0, 0.1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\n  return React.createElement('div', { key: 'dv' + i, style: {\n    position: 'absolute', left: bL + bW * 0.06, top: dy,\n    width: bW * 0.88, height: 1, background: D.text_dim, opacity: dOp\n  }});\n});\n\nreturn React.createElement('div', { style: { position: 'absolute', inset: 0, background: D.bg, overflow: 'hidden' } },\n  React.createElement('div', { key: 'bench', style: {\n    position: 'absolute', left: bL, top: bT, width: bW, height: bH,\n    background: D.surface, borderRadius: 8, border: '1px solid ' + D.text_dim,\n    boxShadow: 'inset 0 2px 25px rgba(0,0,0,0.35), 0 4px 30px rgba(0,0,0,0.5)'\n  }}),\n  ...grain, ...bolts, ...divs, ...rows.filter(Boolean)\n);",
    "audio_anchor": "writes tests for its own code",
    "source_headline": "Stats engrave onto the workbench surface."
  },
  {
    "framesFrom": 1349,
    "framesTo": 1611,
    "code": "const e = React.createElement;\nconst wipeEnd = Math.floor(durationInFrames * 0.45);\nconst matStart = Math.floor(durationInFrames * 0.15);\n\nconst wipeX = interpolate(frame, [0, wipeEnd], [0, width * 1.15], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\nconst chaosOp = interpolate(frame, [matStart, Math.max(matStart + 1, wipeEnd)], [0, 1], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\n\nconst cyanWS = e('div', {style: {\n  position:'absolute', top:0, left:0, width:width, height:height,\n  transform: 'translateX(' + wipeX + 'px)',\n  background: 'linear-gradient(135deg, ' + D.bg + ' 0%, ' + D.surface + ' 100%)',\n  zIndex: 10,\n  display:'flex', alignItems:'center', justifyContent:'center',\n  borderRight: '4px solid ' + D.cyan,\n  boxShadow: '12px 0 60px ' + D.cyan + '55',\n}},\n  e('div', {style: {\n    width: width * 0.55, height: height * 0.5,\n    background: D.surface, borderRadius: 12,\n    border: '2px solid ' + D.cyan, padding: 24,\n    boxShadow: '0 0 30px ' + D.cyan + '33',\n  }},\n    e('div', {style: {display:'flex', gap:8, marginBottom:16}},\n      e('div', {style: {width:12, height:12, borderRadius:'50%', background:D.red}}),\n      e('div', {style: {width:12, height:12, borderRadius:'50%', background:D.amber}}),\n      e('div', {style: {width:12, height:12, borderRadius:'50%', background:D.green}})\n    ),\n    ...Array.from({length:6}, function(_, i) {\n      return e('div', {key:'cl'+i, style: {\n        height:8, width: (35 + i * 10) + '%',\n        background: D.cyan, opacity:0.35, borderRadius:4, marginBottom:8\n      }});\n    })\n  )\n);\n\nconst wins = [\n  {x:0.03, y:0.04, w:0.44, h:0.38, rot:-2, c:D.violet, lbl:'Browser'},\n  {x:0.48, y:0.08, w:0.48, h:0.36, rot:1.5, c:D.amber, lbl:'Spreadsheet'},\n  {x:0.08, y:0.48, w:0.42, h:0.38, rot:1, c:D.green, lbl:'Terminal'},\n  {x:0.44, y:0.52, w:0.52, h:0.4, rot:-1.5, c:D.cyan, lbl:'Code Editor'},\n  {x:0.22, y:0.2, w:0.36, h:0.28, rot:3, c:D.red, lbl:'Desktop'},\n  {x:0.58, y:0.01, w:0.38, h:0.22, rot:-0.8, c:D.white, lbl:'Tabs'}\n];\n\nconst chaosWS = e('div', {style: {\n  position:'absolute', top:0, left:0, width:width, height:height,\n  opacity: chaosOp, background: D.bg,\n}},\n  ...wins.map(function(w, i) {\n    var delay = matStart + i * 3;\n    var s = spring({frame: frame - delay, fps:fps, config:{damping:12, stiffness:160}});\n    var sc = interpolate(s, [0, 1], [0.3, 1], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\n    var floatY = Math.sin((frame + i * 25) * 0.1) * 3;\n    return e('div', {key:'w'+i, style: {\n      position:'absolute',\n      left: w.x * width, top: w.y * height + floatY,\n      width: w.w * width, height: w.h * height,\n      background: D.surface,\n      border: '2px solid ' + w.c,\n      borderRadius: 8,\n      transform: 'scale(' + sc + ') rotate(' + w.rot + 'deg)',\n      boxShadow: '0 4px 24px ' + D.bg + '88',\n      overflow: 'hidden',\n    }},\n      e('div', {style: {\n        height:26, background: w.c + '18',\n        borderBottom: '1px solid ' + w.c + '44',\n        display:'flex', alignItems:'center', padding:'0 10px', gap:6,\n      }},\n        e('div', {style:{width:8,height:8,borderRadius:'50%',background:D.red}}),\n        e('div', {style:{width:8,height:8,borderRadius:'50%',background:D.amber}}),\n        e('div', {style:{width:8,height:8,borderRadius:'50%',background:D.green}}),\n        e('span', {style:{fontFamily:D.font_mono, fontSize:10, color:D.text_dim, marginLeft:8}}, w.lbl)\n      ),\n      e('div', {style:{padding:10}},\n        ...Array.from({length: 3 + (i % 3)}, function(_, k) {\n          return e('div', {key:k, style:{\n            height:5, width: (25 + ((k * 17 + i * 23) % 55)) + '%',\n            background: w.c, opacity:0.2, borderRadius:3, marginBottom:5,\n          }});\n        })\n      )\n    );\n  })\n);\n\nreturn e(React.Fragment, null, chaosWS, cyanWS);",
    "audio_anchor": "Now watch GPT-5.5",
    "source_headline": "Scene WIPES right (CYAN workspace slides away).."
  },
  {
    "framesFrom": 1611,
    "framesTo": 2298,
    "code": "const e = React.createElement;\nconst cx = width / 2;\nconst cy = height / 2;\nconst dur = durationInFrames;\nconst armLen = Math.min(width, height) * 0.34;\nconst bodyR = 55;\nconst bodyScale = spring({ frame, fps, config: { damping: 14, stiffness: 180 } });\nconst tools = [\n  { label: 'BROWSER', text: 'https://api.dev/' },\n  { label: 'CODE', text: 'const data = await' },\n  { label: 'SHEETS', text: 'A1:847 B1:293 C1' },\n  { label: 'DESKTOP', text: 'click > select...' },\n  { label: 'DOCS', text: 'Section 3 drafted' },\n  { label: 'TERMINAL', text: '$ git push origin' },\n  { label: 'CALC', text: '1847 * 29 = 53563' },\n  { label: 'CALENDAR', text: 'Mon Wed Fri sync' }\n];\nconst shades = [1.0, 0.87, 0.74, 0.93, 0.81, 0.68, 0.90, 0.77];\nconst dotGrid = e('div', {\n  style: {\n    position: 'absolute', inset: 0,\n    backgroundImage: 'radial-gradient(circle, ' + D.text_dim + ' 1px, transparent 1px)',\n    backgroundSize: D.dot_grid_spacing + 'px ' + D.dot_grid_spacing + 'px',\n    opacity: D.dot_grid_opacity\n  }\n});\nconst svgK = [];\nconst pulse = 0.5 + 0.5 * Math.sin(frame * 0.08);\nsvgK.push(\n  e('circle', { key: 'g1', cx: cx, cy: cy, r: (bodyR + 22) * bodyScale, fill: 'none', stroke: D.violet, strokeWidth: 1.5, opacity: 0.2 * pulse * bodyScale }),\n  e('circle', { key: 'g2', cx: cx, cy: cy, r: (bodyR + 11) * bodyScale, fill: 'none', stroke: D.violet, strokeWidth: 2, opacity: 0.35 * bodyScale })\n);\nconst pts = Array.from({ length: 8 }, function(_, i) {\n  const a = (i / 8) * Math.PI * 2 - Math.PI / 2;\n  return (cx + Math.cos(a) * bodyR * bodyScale) + ',' + (cy + Math.sin(a) * bodyR * bodyScale);\n}).join(' ');\nsvgK.push(e('polygon', { key: 'body', points: pts, fill: D.bg, stroke: D.violet, strokeWidth: 3, opacity: bodyScale }));\nconst eyeOp = interpolate(frame, [8, 16], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\n[-1, 1].forEach(function(s, idx) {\n  svgK.push(\n    e('circle', { key: 'e' + idx, cx: cx + s * 17 * bodyScale, cy: cy - 7 * bodyScale, r: 7 * bodyScale, fill: D.violet, opacity: eyeOp }),\n    e('circle', { key: 'pu' + idx, cx: cx + s * 17 * bodyScale, cy: cy - 7 * bodyScale, r: 3 * bodyScale, fill: D.bg, opacity: eyeOp })\n  );\n});\nfor (var i = 0; i < 8; i++) {\n  const angle = (i / 8) * Math.PI * 2 - Math.PI / 2;\n  const delay = 5 + i * 2;\n  const ext = spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, stiffness: 150 } });\n  const wb = Math.sin(i * 1.7 + 0.5) * 0.25;\n  const cpX = cx + Math.cos(angle + wb) * armLen * 0.55 * ext;\n  const cpY = cy + Math.sin(angle + wb) * armLen * 0.55 * ext;\n  const tipX = cx + Math.cos(angle) * armLen * ext;\n  const tipY = cy + Math.sin(angle) * armLen * ext;\n  const sw = interpolate(ext, [0, 1], [8, 3.5], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\n  svgK.push(\n    e('path', {\n      key: 'a' + i,\n      d: 'M ' + cx + ' ' + cy + ' Q ' + cpX + ' ' + cpY + ' ' + tipX + ' ' + tipY,\n      stroke: D.violet, strokeWidth: sw, fill: 'none',\n      opacity: shades[i] * ext, strokeLinecap: 'round'\n    }),\n    e('circle', { key: 'tp' + i, cx: tipX, cy: tipY, r: 5 * ext, fill: D.violet, opacity: shades[i] * ext })\n  );\n}\nconst svg = e('svg', {\n  key: 'svg', style: { position: 'absolute', inset: 0 },\n  width: width, height: height, viewBox: '0 0 ' + width + ' ' + height\n}, svgK);\nconst panels = Array.from({ length: 8 }, function(_, i) {\n  const angle = (i / 8) * Math.PI * 2 - Math.PI / 2;\n  const armDel = 5 + i * 2;\n  const panDel = 14 + i * 2;\n  const ext = spring({ frame: Math.max(0, frame - armDel), fps, config: { damping: 12, stiffness: 150 } });\n  const appear = spring({ frame: Math.max(0, frame - panDel), fps, config: { damping: 14, stiffness: 180 } });\n  const tipX = cx + Math.cos(angle) * armLen * ext;\n  const tipY = cy + Math.sin(angle) * armLen * ext;\n  const offX = Math.cos(angle) * 50;\n  const offY = Math.sin(angle) * 34;\n  const typEnd = Math.max(panDel + 1, dur - 10);\n  const chars = Math.floor(interpolate(frame, [panDel, typEnd], [0, tools[i].text.length], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));\n  const txt = tools[i].text.slice(0, chars);\n  const cur = frame % 20 < 10 ? '\\u2588' : '';\n  return e('div', {\n    key: 'pn' + i,\n    style: {\n      position: 'absolute',\n      left: tipX + offX - 62, top: tipY + offY - 25,\n      width: 124, height: 50,\n      background: D.surface,\n      border: '1.5px solid ' + D.violet,\n      borderRadius: 6,\n      display: 'flex', flexDirection: 'column',\n      alignItems: 'center', justifyContent: 'center',\n      opacity: appear,\n      transform: 'scale(' + appear + ')'\n    }\n  },\n    e('span', { style: { color: D.violet, fontSize: 11, fontFamily: D.font_mono, fontWeight: 700, letterSpacing: 1.2, opacity: shades[i] } }, tools[i].label),\n    e('span', { style: { color: D.text_dim, fontSize: 9, fontFamily: D.font_mono, marginTop: 2, whiteSpace: 'nowrap', overflow: 'hidden' } }, txt + cur)\n  );\n});\nreturn e('div', { style: { position: 'absolute', inset: 0, background: D.bg, overflow: 'hidden' } }, dotGrid, svg, ...panels);",
    "audio_anchor": "your whole workflow",
    "source_headline": "A MAGENTA OCTOPUS silhouette enters.."
  },
  {
    "framesFrom": 2298,
    "framesTo": 2358,
    "code": "const h = React.createElement;\nconst clamp = {extrapolateLeft:'clamp',extrapolateRight:'clamp'};\nconst dur = durationInFrames;\nconst cx = width * 0.5;\nconst cy = height * 0.48;\n\nconst tools = [\n  {label:'Browser', angle: 0},\n  {label:'Code', angle: 45},\n  {label:'Spreadsheet', angle: 90},\n  {label:'Email', angle: 135},\n  {label:'Document', angle: 180},\n  {label:'Terminal', angle: 225},\n  {label:'Calendar', angle: 270},\n  {label:'Search', angle: 315}\n];\n\nconst tentacleLen = Math.min(width, height) * 0.28;\nconst bodyR = 52;\n\nconst allCheckDelay = 6;\nconst perItemDelay = 3;\n\nconst moveOnStart = Math.min(dur * 0.55, 50);\nconst bodyDriftX = interpolate(frame, [moveOnStart, Math.max(moveOnStart+1, dur)], [0, width * 0.08], clamp);\nconst bodyDriftY = interpolate(frame, [moveOnStart, Math.max(moveOnStart+1, dur)], [0, -height * 0.03], clamp);\n\nconst tentacles = tools.map((t, i) => {\n  const rad = (t.angle * Math.PI) / 180;\n  const ex = Math.cos(rad) * tentacleLen;\n  const ey = Math.sin(rad) * tentacleLen;\n  const tipX = cx + ex + bodyDriftX;\n  const tipY = cy + ey + bodyDriftY;\n  const midX = cx + ex * 0.5 + Math.sin(frame * 0.08 + i) * 6 + bodyDriftX;\n  const midY = cy + ey * 0.5 + Math.cos(frame * 0.1 + i) * 6 + bodyDriftY;\n  const startX = cx + bodyDriftX;\n  const startY = cy + bodyDriftY;\n\n  const isDoc = t.label === 'Document';\n  const checkIn = allCheckDelay + i * perItemDelay;\n  const checkProg = spring({frame: frame - checkIn, fps, config:{damping:12, stiffness:200}});\n\n  const iconColor = isDoc ? D.red : D.green;\n  const iconText = isDoc ? '\\u26A0\\uFE0F' : '\\u2713';\n  const iconBg = isDoc ? 'rgba(255,60,60,0.15)' : 'rgba(60,255,120,0.12)';\n\n  const wiggle = isDoc ? Math.sin(frame * 0.3) * 3 : 0;\n\n  const pathD = 'M ' + startX + ' ' + startY + ' Q ' + midX + ' ' + midY + ' ' + tipX + ' ' + tipY;\n\n  return h(React.Fragment, {key: i},\n    h('svg', {style:{position:'absolute',left:0,top:0,width:width,height:height,pointerEvents:'none'}},\n      h('path', {d: pathD, stroke: isDoc ? D.red : D.violet, strokeWidth: 4, fill:'none', opacity: 0.6,\n        strokeDasharray: isDoc ? '8 4' : 'none'})\n    ),\n    h('div', {style:{\n      position:'absolute',\n      left: tipX - 38,\n      top: tipY - 38 + wiggle,\n      width: 76, height: 76,\n      borderRadius: 14,\n      background: D.surface,\n      border: '2px solid ' + (isDoc ? D.red : D.violet),\n      display:'flex', flexDirection:'column', alignItems:'center', justifyContent:'center',\n      opacity: interpolate(frame, [0, 8], [0, 1], clamp),\n      boxShadow: isDoc ? '0 0 18px ' + D.red : 'none'\n    }},\n      h('span', {style:{fontSize:13, color: D.text_dim, fontFamily: D.font_mono, marginBottom:2}}, t.label),\n      h('span', {style:{\n        fontSize: 22,\n        color: iconColor,\n        opacity: checkProg,\n        transform: 'scale(' + checkProg + ')',\n        fontWeight:'bold'\n      }}, iconText)\n    )\n  );\n});\n\nconst bodyScale = spring({frame: frame, fps, config:{damping:16, stiffness:140}});\n\nconst eyeOffX = interpolate(frame, [moveOnStart, Math.max(moveOnStart+1, dur)], [0, 6], clamp);\n\nconst body = h('div', {style:{\n  position:'absolute',\n  left: cx - bodyR + bodyDriftX,\n  top: cy - bodyR + bodyDriftY,\n  width: bodyR * 2, height: bodyR * 2,\n  borderRadius: '50%',\n  background: 'radial-gradient(circle at 40% 35%, ' + D.violet + ', ' + D.surface + ')',\n  border: '3px solid ' + D.violet,\n  transform: 'scale(' + bodyScale + ')',\n  display:'flex', alignItems:'center', justifyContent:'center',\n  boxShadow: '0 0 30px ' + D.violet\n}},\n  h('div', {style:{display:'flex', gap:16}},\n    h('div', {style:{width:16,height:16,borderRadius:'50%',background:D.bg,position:'relative',overflow:'hidden'}},\n      h('div', {style:{width:8,height:8,borderRadius:'50%',background:D.white,position:'absolute',top:4,left:4+eyeOffX}})\n    ),\n    h('div', {style:{width:16,height:16,borderRadius:'50%',background:D.bg,position:'relative',overflow:'hidden'}},\n      h('div', {style:{width:8,height:8,borderRadius:'50%',background:D.white,position:'absolute',top:4,left:4+eyeOffX}})\n    )\n  )\n);\n\nconst textStart = Math.min(dur * 0.5, 45);\nconst textProg = spring({frame: frame - textStart, fps, config:{damping:14, stiffness:180}});\nconst quoteOp = interpolate(frame, [textStart, Math.min(textStart + 10, dur - 1)], [0, 1], clamp);\n\nconst quote = h('div', {style:{\n  position:'absolute',\n  bottom: height * 0.08,\n  left: 0, right: 0,\n  textAlign:'center',\n  opacity: quoteOp,\n  transform: 'translateY(' + ((1 - textProg) * 30) + 'px)'\n}},\n  h('span', {style:{\n    fontFamily: D.font_display,\n    fontSize: 42,\n    fontWeight: 900,\n    color: D.violet,\n    letterSpacing: 3,\n    textShadow: '0 0 24px ' + D.violet + ', 0 0 48px ' + D.violet\n  }}, '\"YOU NEVER NEED TO LEAVE.\"')\n);\n\nconst bg = h('div', {style:{\n  position:'absolute', inset:0,\n  background: D.bg\n}});\n\nreturn h(React.Fragment, null, bg, ...tentacles, body, quote);",
    "audio_anchor": "you never need to leave",
    "source_headline": "All 8 tasks complete.."
  },
  {
    "framesFrom": 2358,
    "framesTo": 2485,
    "code": "const stats = [{label:'Terminal-Bench',value:'82.7%',glow:false},{label:'Hallucination',value:'86%',glow:true},{label:'Pricing',value:'$5 / $30',glow:false},{label:'Token Savings',value:'-72% tokens',glow:false}];const etchProgress = interpolate(frame,[0,Math.max(1,durationInFrames*0.6)],[0,1],{extrapolateLeft:'clamp',extrapolateRight:'clamp'});const deskGrain = [];for(let i=0;i<40;i++){deskGrain.push(React.createElement('div',{key:'g'+i,style:{position:'absolute',left:Math.random()*width,top:Math.random()*height,width:Math.random()*300+50,height:1,background:resolveColor('text_dim'),opacity:0.06,transform:'rotate('+(Math.random()*4-2)+'deg)'}}))}return React.createElement('div',{style:{position:'absolute',inset:0,background:D.bg,overflow:'hidden'}},React.createElement('div',{style:{position:'absolute',inset:0,background:'linear-gradient(145deg, '+D.surface+' 0%, '+D.bg+' 100%)'}}),...deskGrain,React.createElement('div',{style:{position:'absolute',inset:0,background:'radial-gradient(ellipse at 50% 50%, transparent 30%, '+D.bg+' 90%)',opacity:0.5}}),React.createElement('div',{style:{position:'absolute',top:height*0.08,left:width*0.5,transform:'translateX(-50%)',fontFamily:D.font_display,fontSize:28,color:D.text_dim,letterSpacing:6,textTransform:'uppercase',opacity:interpolate(frame,[0,15],[0,0.6],{extrapolateLeft:'clamp',extrapolateRight:'clamp'})}},'Claude Opus 4 Stats'),React.createElement('div',{style:{position:'absolute',top:height*0.22,left:0,right:0,display:'flex',flexDirection:'column',alignItems:'center',gap:18}},stats.map(function(s,i){const delay=i*8;const prog=interpolate(frame,[delay,Math.max(delay+1,delay+20)],[0,1],{extrapolateLeft:'clamp',extrapolateRight:'clamp'});const clipX=prog*100;const glowPulse=s.glow?0.5+0.5*Math.sin((frame-delay)*0.15):0;const baseColor=s.glow?D.red:D.cyan;const dimColor=s.glow?D.red:D.text_dim;return React.createElement('div',{key:i,style:{position:'relative',width:width*0.7,padding:'14px 24px',opacity:interpolate(prog,[0,0.3],[0,1],{extrapolateLeft:'clamp',extrapolateRight:'clamp'})}},React.createElement('div',{style:{position:'absolute',inset:0,border:'1px solid '+dimColor,opacity:0.2*prog,borderRadius:4}}),React.createElement('div',{style:{position:'absolute',bottom:0,left:0,height:2,width:clipX+'%',background:baseColor,opacity:0.4+glowPulse*0.4,boxShadow:s.glow?'0 0 20px '+D.red+', 0 0 40px '+D.red:'none',borderRadius:1}}),React.createElement('div',{style:{display:'flex',justifyContent:'space-between',alignItems:'baseline',clipPath:'inset(0 '+(100-clipX)+'% 0 0)'}},React.createElement('span',{style:{fontFamily:D.font_mono,fontSize:22,color:D.text_dim,letterSpacing:2}},s.label),React.createElement('span',{style:{fontFamily:D.font_mono,fontSize:s.glow?36:30,fontWeight:'bold',color:baseColor,textShadow:s.glow?'0 0 12px '+D.red+', 0 0 30px '+D.red:'0 0 8px '+resolveColor('cyan'),letterSpacing:1}},s.value)),React.createElement('div',{style:{position:'absolute',inset:0,pointerEvents:'none',background:s.glow?'radial-gradient(ellipse at center, '+D.red+' 0%, transparent 70%)':'none',opacity:glowPulse*0.08}}))})),React.createElement('div',{style:{position:'absolute',bottom:height*0.06,left:width*0.5,transform:'translateX(-50%)',display:'flex',gap:6}},Array.from({length:30}).map(function(_,i){return React.createElement('div',{key:i,style:{width:2,height:interpolate(Math.sin(frame*0.1+i*0.5),[-1,1],[4,14]),background:D.cyan,opacity:0.15+etchProgress*0.1}})})))",
    "audio_anchor": "confirmed it solved",
    "source_headline": "Stats etch onto desk."
  },
  {
    "framesFrom": 2485,
    "framesTo": 2758,
    "code": "const t = frame / fps;\nconst dur = durationInFrames;\nconst mid = width * 0.5;\n\n// Split line animation\nconst lineY = interpolate(frame, [0, 12], [height, 0], {extrapolateLeft:'clamp',extrapolateRight:'clamp'});\n\n// Left/right fade\nconst leftOp = interpolate(frame, [6, 18], [0, 1], {extrapolateLeft:'clamp',extrapolateRight:'clamp'});\nconst rightOp = interpolate(frame, [12, 24], [0, 1], {extrapolateLeft:'clamp',extrapolateRight:'clamp'});\n\n// Labels\nconst labelOp = interpolate(frame, [20, 30], [0, 1], {extrapolateLeft:'clamp',extrapolateRight:'clamp'});\n\n// Architect body (left side)\nconst archX = width * 0.25;\nconst archY = height * 0.42;\nconst archScale = spring({frame: Math.max(0, frame - 8), fps, config:{damping:14,stiffness:180}});\n\n// Cube float\nconst cubeFloat = Math.sin(frame * 0.08) * 4;\nconst cubeGlow = interpolate(frame, [18, 28], [0, 1], {extrapolateLeft:'clamp',extrapolateRight:'clamp'});\nconst checkOp = interpolate(frame, [28, 36], [0, 1], {extrapolateLeft:'clamp',extrapolateRight:'clamp'});\n\n// Octopus (right side)\nconst octoX = width * 0.75;\nconst octoY = height * 0.42;\nconst octoScale = spring({frame: Math.max(0, frame - 14), fps, config:{damping:14,stiffness:180}});\n\nconst taskIcons = ['</>','DB','API','UI','DOC','TEST','LOG','DEPLOY'];\nconst warningIdx = 7;\nconst tentacles = Array.from({length:8}, (_, i) => {\n  const angle = (i / 8) * Math.PI * 2 - Math.PI / 2;\n  const waveOff = Math.sin(frame * 0.06 + i * 0.8) * 8;\n  const rad = 110 + waveOff;\n  const tx = Math.cos(angle) * rad;\n  const ty = Math.sin(angle) * rad;\n  const delay = 18 + i * 3;\n  const tentOp = interpolate(frame, [delay, Math.max(delay+1, delay+8)], [0,1], {extrapolateLeft:'clamp',extrapolateRight:'clamp'});\n  const isWarning = i === warningIdx;\n  const flagWave = isWarning ? Math.sin(frame * 0.15) * 6 : 0;\n  return React.createElement('g', {key:i, opacity: tentOp}, \n    React.createElement('line', {x1:0, y1:0, x2:tx, y2:ty, stroke: resolveColor(isWarning ? 'amber' : 'violet'), strokeWidth:3, strokeLinecap:'round', opacity:0.6}),\n    React.createElement('circle', {cx:tx, cy:ty, r:18, fill: resolveColor(isWarning ? 'amber' : 'surface'), stroke: resolveColor(isWarning ? 'amber' : 'violet'), strokeWidth:2}),\n    React.createElement('text', {x:tx, y:ty + 1, fill: resolveColor(isWarning ? 'bg' : 'cyan'), fontSize:8, fontFamily:D.font_mono, textAnchor:'middle', dominantBaseline:'middle'}, taskIcons[i]),\n    isWarning ? React.createElement('g', {transform:'translate('+(tx + flagWave)+','+(ty - 28)+')'},\n      React.createElement('line', {x1:0, y1:0, x2:0, y2:-14, stroke:resolveColor('amber'), strokeWidth:2}),\n      React.createElement('polygon', {points:'0,-14 12,-10 0,-6', fill:resolveColor('amber')}),\n      React.createElement('text', {x:5, y:-8, fill:resolveColor('bg'), fontSize:6, fontFamily:D.font_mono, fontWeight:'bold', textAnchor:'middle', dominantBaseline:'middle'}, '!')\n    ) : null\n  );\n});\n\n// Build left side - architect\nconst leftSide = React.createElement('div', {style:{position:'absolute', left:0, top:0, width:mid, height:height, opacity:leftOp, overflow:'hidden'}},\n  React.createElement('svg', {width:mid, height:height, viewBox:'0 0 '+mid+' '+height},\n    React.createElement('g', {transform:'translate('+archX+','+archY+') scale('+archScale+')'},\n      // Hard hat\n      React.createElement('path', {d:'M-22,-45 L22,-45 L26,-35 L-26,-35 Z', fill:resolveColor('amber'), opacity:0.9}),\n      React.createElement('rect', {x:-30, y:-35, width:60, height:6, rx:2, fill:resolveColor('amber')}),\n      // Head\n      React.createElement('circle', {cx:0, cy:-18, r:16, fill:resolveColor('surface'), stroke:resolveColor('text'), strokeWidth:2}),\n      // Eyes\n      React.createElement('circle', {cx:-5, cy:-20, r:2, fill:resolveColor('cyan')}),\n      React.createElement('circle', {cx:5, cy:-20, r:2, fill:resolveColor('cyan')}),\n      // Body\n      React.createElement('rect', {x:-20, y:0, width:40, height:50, rx:6, fill:resolveColor('surface'), stroke:resolveColor('cyan'), strokeWidth:2}),\n      // Arms\n      React.createElement('line', {x1:-20, y1:10, x2:-40, y2:35, stroke:resolveColor('text'), strokeWidth:4, strokeLinecap:'round'}),\n      React.createElement('line', {x1:20, y1:10, x2:40, y2:35, stroke:resolveColor('text'), strokeWidth:4, strokeLinecap:'round'})\n    ),\n    // Verified cube floating above hand\n    React.createElement('g', {transform:'translate('+(archX+50)+','+(archY+20+cubeFloat)+')'},\n      React.createElement('rect', {x:-22, y:-22, width:44, height:44, rx:4, fill:'none', stroke:resolveColor('cyan'), strokeWidth:2.5, filter: cubeGlow > 0 ? undefined : undefined}),\n      React.createElement('rect', {x:-22, y:-22, width:44, height:44, rx:4, fill:resolveColor('cyan'), opacity:cubeGlow*0.15}),\n      // 3D cube lines\n      React.createElement('line', {x1:-14, y1:-14, x2:-4, y2:-20, stroke:resolveColor('cyan'), strokeWidth:1.5, opacity:0.5}),\n      React.createElement('line', {x1:14, y1:-14, x2:24, y2:-20, stroke:resolveColor('cyan'), strokeWidth:1.5, opacity:0.5}),\n      React.createElement('line', {x1:14, y1:14, x2:24, y2:8, stroke:resolveColor('cyan'), strokeWidth:1.5, opacity:0.5}),\n      React.createElement('line', {x1:24, y1:-20, x2:24, y2:8, stroke:resolveColor('cyan'), strokeWidth:1.5, opacity:0.5}),\n      // Checkmark\n      React.createElement('path', {d:'M-8,2 L-2,8 L10,-6', fill:'none', stroke:resolveColor('green'), strokeWidth:3, strokeLinecap:'round', strokeLinejoin:'round', opacity:checkOp})\n    )\n  ),\n  // Left label\n  React.createElement('div', {style:{position:'absolute', bottom:height*0.12, left:0, width:'100%', textAlign:'center', opacity:labelOp}},\n    React.createElement('div', {style:{fontFamily:D.font_display, fontSize:28, color:resolveColor('cyan'), fontWeight:'bold', letterSpacing:2}}, 'SPECIALIST'),\n    React.createElement('div', {style:{fontFamily:D.font_mono, fontSize:14, color:resolveColor('text_dim'), marginTop:6}}, 'Verified output')\n  )\n);\n\n// Build right side - octopus\nconst rightSide = React.createElement('div', {style:{position:'absolute', left:mid, top:0, width:mid, height:height, opacity:rightOp, overflow:'hidden'}},\n  React.createElement('svg', {width:mid, height:height, viewBox:'0 0 '+mid+' '+height},\n    React.createElement('g', {transform:'translate('+(mid*0.5)+','+octoY+') scale('+octoScale+')'},\n      // Octopus body\n      React.createElement('ellipse', {cx:0, cy:-10, rx:40, ry:32, fill:resolveColor('violet'), opacity:0.85}),\n      React.createElement('ellipse', {cx:0, cy:-10, rx:40, ry:32, fill:'none', stroke:resolveColor('violet'), strokeWidth:2}),\n      // Eyes\n      React.createElement('circle', {cx:-12, cy:-16, r:6, fill:resolveColor('white')}),\n      React.createElement('circle', {cx:12, cy:-16, r:6, fill:resolveColor('white')}),\n      React.createElement('circle', {cx:-10, cy:-16, r:3, fill:resolveColor('bg')}),\n      React.createElement('circle', {cx:14, cy:-16, r:3, fill:resolveColor('bg')}),\n      // Tentacles + tasks\n      ...tentacles\n    )\n  ),\n  // Right label\n  React.createElement('div', {style:{position:'absolute', bottom:height*0.12, left:0, width:'100%', textAlign:'center', opacity:labelOp}},\n    React.createElement('div', {style:{fontFamily:D.font_display, fontSize:28, color:resolveColor('violet'), fontWeight:'bold', letterSpacing:2}}, 'GENERALIST'),\n    React.createElement('div', {style:{fontFamily:D.font_mono, fontSize:14, color:resolveColor('text_dim'), marginTop:6}}, 'Does everything')\n  )\n);\n\n// Divider line\nconst divider = React.createElement('div', {style:{position:'absolute', left:mid-1, top:0, width:2, height:height, background:resolveColor('text_dim'), opacity:0.3, transform:'translateY('+lineY+'px)'}});\n\n// VS badge\nconst vsOp = interpolate(frame, [16, 24], [0,1], {extrapolateLeft:'clamp',extrapolateRight:'clamp'});\nconst vsScale = spring({frame: Math.max(0, frame-18), fps, config:{damping:14,stiffness:180}});\nconst vsBadge = React.createElement('div', {style:{position:'absolute', left:mid-28, top:height*0.38, width:56, height:56, borderRadius:'50%', background:resolveColor('bg'), border:'2px solid '+resolveColor('text_dim'), display:'flex', alignItems:'center', justifyContent:'center', opacity:vsOp, transform:'scale('+vsScale+')'}},\n  React.createElement('span', {style:{fontFamily:D.font_display, fontSize:20, color:resolveColor('text'), fontWeight:'bold'}}, 'VS')\n);\n\nreturn React.createElement('div', {style:{position:'absolute', inset:0, background:resolveColor('bg')}}, leftSide, rightSide, divider, vsBadge);",
    "audio_anchor": "specialist you trust",
    "source_headline": "Split screen.."
  },
  {
    "framesFrom": 2758,
    "framesTo": 2874,
    "code": "const e = React.createElement;\nconst del = Math.round(fps * 0.5);\nconst t1 = 0;\nconst t2 = del;\nconst t3 = del * 2;\n\nconst consultantProgress = spring({frame: frame - t1, fps, config:{damping:D.spring_damping, stiffness:D.spring_stiffness}});\nconst osProgress = spring({frame: frame - t2, fps, config:{damping:D.spring_damping, stiffness:D.spring_stiffness}});\nconst taglineProgress = spring({frame: frame - t3, fps, config:{damping:D.spring_damping, stiffness:D.spring_stiffness}});\n\nconst arrowOpacity = interpolate(frame, [t2, Math.max(t2+1, t2+8)], [0,1], {extrapolateLeft:'clamp',extrapolateRight:'clamp'});\n\nconst consultantX = interpolate(consultantProgress, [0,1], [-120, 0]);\nconst osX = interpolate(osProgress, [0,1], [120, 0]);\nconst taglineY = interpolate(taglineProgress, [0,1], [30, 0]);\n\nreturn e(AbsoluteFill, {style:{backgroundColor:D.bg, display:'flex', flexDirection:'column', alignItems:'center', justifyContent:'center'}},\n  e('div', {style:{display:'flex', alignItems:'center', gap:32, marginBottom:48}},\n    e('span', {style:{\n      fontFamily:D.font_display,\n      fontSize:52,\n      fontWeight:900,\n      color:D.cyan,\n      letterSpacing:6,\n      opacity:consultantProgress,\n      transform:'translateX('+consultantX+'px)'\n    }}, 'CONSULTANT'),\n    e('span', {style:{\n      fontFamily:D.font_mono,\n      fontSize:36,\n      color:D.text_dim,\n      opacity:arrowOpacity,\n      marginLeft:8,\n      marginRight:8\n    }}, '\\u2190  \\u2192'),\n    e('span', {style:{\n      fontFamily:D.font_display,\n      fontSize:52,\n      fontWeight:900,\n      color:D.violet,\n      letterSpacing:6,\n      opacity:osProgress,\n      transform:'translateX('+osX+'px)'\n    }}, 'OPERATING SYSTEM')\n  ),\n  e('div', {style:{\n    fontFamily:D.font_display,\n    fontSize:Math.round(52*1.3),\n    fontWeight:900,\n    color:D.amber,\n    textAlign:'center',\n    opacity:taglineProgress,\n    transform:'translateY('+taglineY+'px)',\n    letterSpacing:3,\n    maxWidth:width*0.85\n  }}, \"THEY'RE NOT IN THE SAME RACE.\")\n);",
    "audio_anchor": "Get that distinction",
    "source_headline": "Center text builds."
  }
]