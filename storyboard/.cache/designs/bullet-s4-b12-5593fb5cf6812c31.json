{
  "code": "const e = React.createElement;\nconst totalChars = 19;\nconst typeStartFrame = Math.round(fps * 0.35);\nconst charsPerFrame = (D.type_speed_cps || 18) / fps;\n\nconst slideUp = interpolate(frame, [0, Math.round(fps * 0.45)], [height * 0.35, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\nconst panelOpacity = interpolate(frame, [0, Math.round(fps * 0.3)], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });\n\nconst visibleChars = Math.min(totalChars, Math.max(0, Math.floor((frame - typeStartFrame) * charsPerFrame)));\nconst headerText = 'WHAT DEVELOPERS SAY'.slice(0, visibleChars);\n\nconst cursorOn = frame >= typeStartFrame && (visibleChars < totalChars || Math.floor(frame / 8) % 2 === 0);\nconst cursorOpacity = cursorOn ? 1 : 0;\n\nconst glowPulse = visibleChars >= totalChars ? interpolate(frame % Math.round(fps * 1.5), [0, Math.round(fps * 0.75), Math.round(fps * 1.5)], [0.15, 0.4, 0.15], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) : 0;\n\nconst lineScale = visibleChars >= totalChars ? spring({ frame: frame - typeStartFrame - Math.round(totalChars / charsPerFrame), fps, config: { damping: D.spring_damping, stiffness: D.spring_stiffness } }) : 0;\n\nconst lineWidth = lineScale * width * 0.45;\n\nreturn e('div', { style: { position: 'absolute', inset: 0, backgroundColor: D.bg, overflow: 'hidden' } },\n  e('div', { style: { position: 'absolute', inset: 0, backgroundImage: 'radial-gradient(circle at ' + (width * 0.5) + 'px ' + (height * 0.5) + 'px, ' + resolveColor('surface') + ' 0%, ' + resolveColor('bg') + ' 70%)', opacity: 0.5 } }),\n\n  e('div', { style: { position: 'absolute', left: 0, right: 0, top: 0, bottom: 0, transform: 'translateY(' + slideUp + 'px)', opacity: panelOpacity, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' } },\n\n    e('div', { style: { position: 'relative', padding: '48px 80px', borderRadius: 16, border: '1.5px solid ' + resolveColor('surface'), backgroundColor: resolveColor('bg'), boxShadow: '0 0 ' + Math.round(glowPulse * 60) + 'px ' + resolveColor('cyan') } },\n\n      e('div', { style: { display: 'flex', alignItems: 'center', justifyContent: 'center' } },\n        e('span', { style: { fontFamily: D.font_display, fontSize: 72, fontWeight: 800, color: D.white, letterSpacing: 6, lineHeight: 1.1, textShadow: '0 0 20px ' + resolveColor('white') + '44' } }, headerText),\n        e('span', { style: { display: 'inline-block', width: 4, height: 68, backgroundColor: D.white, marginLeft: 4, opacity: cursorOpacity, transition: 'none' } })\n      ),\n\n      e('div', { style: { position: 'absolute', bottom: -18, left: '50%', transform: 'translateX(-50%)', height: 3, width: lineWidth, backgroundColor: D.cyan, borderRadius: 2, boxShadow: '0 0 12px ' + resolveColor('cyan') } })\n    ),\n\n    e('div', { style: { marginTop: 28, fontFamily: D.font_mono, fontSize: 16, color: D.text_dim, letterSpacing: 3, opacity: interpolate(frame, [Math.round(fps * 1.2), Math.min(Math.round(fps * 1.2) + 1, durationInFrames - 1)], [0, 0.6], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) } }, 'REAL-WORLD FEEDBACK')\n  )\n);",
  "audio_anchor": "devs who use these",
  "source_headline": "Scoreboard slides up.."
}