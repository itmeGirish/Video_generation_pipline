{
  "code": "const e = React.createElement;\nconst trackTop = height * 0.38;\nconst trackH = height * 0.32;\nconst trackLeft = width * 0.08;\nconst trackRight = width * 0.88;\nconst trackW = trackRight - trackLeft;\nconst finishX = trackRight;\nconst dur = durationInFrames;\nconst safeEnd = Math.max(1, dur - 1);\n\n/* Slow-motion zone: last 10% of duration uses stretched frames */\nconst slowZoneStart = Math.floor(dur * 0.75);\nconst slowFrames = dur - slowZoneStart;\nconst isSlowZone = frame >= slowZoneStart;\nconst effectiveProgress = isSlowZone\n  ? (slowZoneStart + (frame - slowZoneStart) * 0.25) / dur\n  : frame / dur;\nconst raceProgress = Math.min(effectiveProgress * 1.33, 1.0);\n\n/* Runner positions (percent of track) */\nconst magentaTarget = 0.787;\nconst cyanTarget = 0.780;\nconst magentaPos = Math.min(raceProgress, 1.0) * magentaTarget;\nconst cyanPos = Math.min(raceProgress, 1.0) * cyanTarget;\nconst magentaX = trackLeft + magentaPos * trackW;\nconst cyanX = trackLeft + cyanPos * trackW;\n\n/* Title animation */\nconst titleOp = interpolate(frame, [0, Math.max(1, fps * 0.3)], [0, 1], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\nconst titleY = interpolate(frame, [0, Math.max(1, fps * 0.3)], [-30, 0], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\n\n/* Finish line pulse when in slow zone */\nconst finishPulse = isSlowZone ? 0.5 + 0.5 * Math.sin(frame * 0.4) : 0.3;\n\n/* Delta chip appears near end */\nconst deltaStart = Math.floor(dur * 0.85);\nconst deltaOp = interpolate(frame, [deltaStart, Math.max(deltaStart + 1, deltaStart + fps * 0.3)], [0, 0.55], {extrapolateLeft:'clamp', extrapolateRight:'clamp'});\n\n/* Lane lines */\nconst laneCount = 5;\nconst lanes = Array.from({length: laneCount}, (_, i) => {\n  const y = trackTop + (i / (laneCount - 1)) * trackH;\n  return e('div', {key: 'lane' + i, style: {position:'absolute', left: trackLeft, top: y, width: trackW, height: 1, background: D.text_dim, opacity: 0.2}});\n});\n\n/* Distance markers */\nconst markers = Array.from({length: 5}, (_, i) => {\n  const pct = (i + 1) * 20;\n  const x = trackLeft + (pct / 100) * trackW;\n  return e('div', {key: 'mk' + i, style: {position:'absolute', left: x, top: trackTop - 18, fontFamily: D.font_mono, fontSize: 11, color: D.text_dim, opacity: 0.4, transform: 'translateX(-50%)'}}, pct + '%');\n});\n\n/* Finish line */\nconst finishLine = e('div', {style: {position:'absolute', left: finishX, top: trackTop - 10, width: 3, height: trackH + 20, background: D.white, opacity: finishPulse, borderRadius: 2}});\n\n/* Runner helper */\nconst runner = (label, color, x, y, score) => {\n  const runnerSize = 18;\n  const bobble = Math.sin(frame * 0.6 + (label === 'GPT' ? 0 : 2)) * (isSlowZone ? 1 : 3);\n  return e(React.Fragment, null,\n    /* Trail */\n    e('div', {style: {position:'absolute', left: trackLeft, top: y - 2, width: Math.max(0, x - trackLeft), height: 4, borderRadius: 2, background: 'linear-gradient(90deg, transparent, ' + resolveColor(color) + ')', opacity: 0.35}}),\n    /* Runner dot */\n    e('div', {style: {position:'absolute', left: x - runnerSize / 2, top: y - runnerSize / 2 + bobble, width: runnerSize, height: runnerSize, borderRadius: '50%', background: resolveColor(color), boxShadow: '0 0 12px ' + resolveColor(color)}}),\n    /* Label */\n    e('div', {style: {position:'absolute', left: x - 30, top: y - runnerSize / 2 - 22 + bobble, width: 60, textAlign: 'center', fontFamily: D.font_mono, fontSize: 12, color: resolveColor(color), fontWeight: 700}}, label),\n    /* Score (appears when close to finish) */\n    raceProgress > 0.8 ? e('div', {style: {position:'absolute', left: x - 25, top: y + runnerSize / 2 + 6 + bobble, width: 50, textAlign: 'center', fontFamily: D.font_mono, fontSize: 11, color: resolveColor(color), opacity: interpolate(raceProgress, [0.8, 0.9], [0, 1], {extrapolateLeft:'clamp', extrapolateRight:'clamp'})}}, score + '%') : null\n  );\n};\n\nconst magentaY = trackTop + trackH * 0.35;\nconst cyanY = trackTop + trackH * 0.65;\n\n/* Slow-mo overlay */\nconst slowMoLabel = isSlowZone ? e('div', {style: {position:'absolute', right: width * 0.06, top: trackTop - 40, fontFamily: D.font_mono, fontSize: 13, color: D.amber, opacity: 0.5 + 0.3 * Math.sin(frame * 0.3), letterSpacing: 2}}, 'SLOW-MO ▶ 0.25x') : null;\n\n/* Delta chip */\nconst deltaChip = e('div', {style: {position:'absolute', left: finishX + 14, top: (magentaY + cyanY) / 2 - 10, fontFamily: D.font_mono, fontSize: 10, color: D.text_dim, opacity: deltaOp, padding: '2px 6px', border: '1px solid ' + D.text_dim, borderRadius: 4, letterSpacing: 1}}, '+0.7');\n\nreturn e('div', {style: {position:'absolute', inset: 0, background: D.bg, overflow: 'hidden'}},\n  /* Title */\n  e('div', {style: {position:'absolute', top: height * 0.06, left: 0, width: width, textAlign: 'center', opacity: titleOp, transform: 'translateY(' + titleY + 'px)'}},\n    e('div', {style: {fontFamily: D.font_display, fontSize: 38, color: D.white, fontWeight: 800, letterSpacing: 2}}, 'RACE 2 — OSWORLD'),\n    e('div', {style: {fontFamily: D.font_mono, fontSize: 16, color: D.amber, marginTop: 6, letterSpacing: 4, opacity: 0.8}}, 'PHOTO FINISH')\n  ),\n  /* Track area */\n  ...lanes,\n  ...markers,\n  finishLine,\n  runner('GPT', 'violet', magentaX, magentaY, '78.7'),\n  runner('CLAUDE', 'cyan', cyanX, cyanY, '78.0'),\n  slowMoLabel,\n  deltaChip,\n  /* Bottom label */\n  e('div', {style: {position:'absolute', bottom: height * 0.06, left: 0, width: width, textAlign: 'center', fontFamily: D.font_mono, fontSize: 13, color: D.text_dim, opacity: 0.5}}, 'Computer-use benchmark · Desktop automation')\n);",
  "audio_anchor": "basically a photo finish",
  "source_headline": "RACE 2 — OSWORLD (PHOTO FINISH).."
}